# QA Test Case Document — Step 1: Application, App Service and Database Environment Setup

## Header

| Field | Value |
|---|---|
| Application | Web |
| Step / Phase | Step 1 — Application, App Service and Database Environment Setup |
| BRD reference(s) | Web BRD Sections 1, 2.1, 2.2, 5 |
| Implementation Sequence reference | Web Implementation Sequence Step 1.1–1.5 |
| Date | 2026-09-16 |
| Tested by | Claude Code (self-tested; independent architect review pending) |
| Environment | Azure: `app-sportspres-assetmgmt` (production) + `dev` slot, `mysql-sportspres-assetmgmt` (databases `assetmgmt` / `assetmgmt_dev`), `sasportspresentation` storage account. Local: `az login` session used in place of Managed Identity for developer-side smoke testing. |

## Summary

| Total cases | Passed | Failed | Blocked | Not yet run |
|---|---|---|---|---|
| 17 | 16 | 0 | 0 | 1 |

Any Failed or Blocked case must have a corresponding note in "Open defects / follow-ups" below.

## Test cases

| ID | Description | Preconditions | Steps | Expected result | Actual result | Status | Notes |
|---|---|---|---|---|---|---|---|
| TC-01 | App Service Plan provisioned, Linux, Standard tier | Azure access confirmed | 1. `az appservice plan show -n asp-sportspres-assetmgmt` | Plan exists, `reserved: true` (Linux), SKU S1 | Confirmed via `az appservice plan create` output: Linux, Standard, `provisioningState: Succeeded` | Pass | |
| TC-02 | Production App Service + dev deployment slot exist | TC-01 passed | 1. `az webapp list --resource-group rgsportspresentationsource` | `app-sportspres-assetmgmt` and its `dev` slot both exist and are `Running` | Both created; hostnames `app-sportspres-assetmgmt.azurewebsites.net` and `-dev.azurewebsites.net` | Pass | |
| TC-03 | HTTPS-only enforced, min TLS 1.2, FTP disabled | TC-02 passed | 1. `az webapp config show` on both prod and dev | `httpsOnly: true`, `minTlsVersion: 1.2`, `ftpsState: Disabled` | Confirmed on both slots | Pass | |
| TC-04 | System-assigned Managed Identity exists per slot | TC-02 passed | 1. `az webapp identity show` on prod and dev | Each slot has its own distinct `principalId` | Prod `33a4e55a-...`, dev `375425bf-...` — confirmed distinct | Pass | |
| TC-05 | Managed Identity has least-privilege Storage RBAC | TC-04 passed | 1. `az role assignment list --scope <storage account>` | Both identities hold **Storage Blob Data Contributor**, scoped to the storage account only (not subscription/RG) | Confirmed for both principals, scope = storage account resource ID | Pass | |
| TC-06 | App Service → Storage connectivity via Managed Identity, no keys anywhere | TC-05 passed | 1. Deploy to the `dev` slot 2. Hit `https://app-sportspres-assetmgmt-dev.azurewebsites.net/api/health/storage` | `{ ok: true }`, no `AZURE_STORAGE_KEY`/connection string present in App Service settings | **Confirmed against the live deployed dev slot**: `{"ok":true}` — this is the App Service's own Managed Identity (principal `375425bf-...`), not the developer's identity, proving TC-05's RBAC grant actually works end-to-end. | Pass | |
| TC-06b | Local dev proves the *code path* against Storage | — | 1. `node src/index.js` locally with `.env` pointing at real storage/DB, using `az login` credential | `/api/health/storage` returns `{ ok: true }` | Confirmed `{ ok: true }` | Pass | Proves the SDK/credential-chain code is correct; does not by itself prove the App Service's identity (different principal) has sufficient rights — TC-05's explicit role assignment is the evidence for that. |
| TC-07 | MySQL Flexible Server provisioned, General Purpose, Standard tier, firewall (not VNet) | Azure access confirmed | 1. `az mysql flexible-server show` | `state: Ready`, tier `GeneralPurpose`, SKU `Standard_D2ds_v4`, firewall rule `AllowAllAzureServicesAndResourcesWithinAzureIps` present | Confirmed | Pass | |
| TC-08 | Two databases exist on the one shared server (prod + dev) | TC-07 passed | 1. Connect as AAD admin, `SHOW DATABASES` | `assetmgmt` and `assetmgmt_dev` both exist | Confirmed via `create-databases.mjs` | Pass | |
| TC-09 | Full schema applied to both databases | TC-08 passed | 1. Run `scripts/migrate.mjs` against each database | `users`, `events`, `event_tables` tables created, migrations tracked in `schema_migrations` | Confirmed — `migrate.mjs` output showed all 3 migrations applied to both `assetmgmt` and `assetmgmt_dev` | Pass | |
| TC-10 | `event_tables` enforces Web BRD Section 3.1 LED rules at the DB layer | TC-09 passed | 1. Attempt `INSERT` with `outer_led=1, inner_led=0, main_led=0` (outer alone) 2. Attempt `INSERT` with `outer_led=1, main_led=1, inner_led=0` (outer+main, no inner) 3. Attempt `INSERT` with all three `= 0` 4. Attempt each valid combination (inner alone, inner+outer, inner+main, inner+outer+main, main alone) | Invalid combinations rejected by `CHECK` constraints; all valid combinations succeed | **Superseded by migration 004** (see D-08): the original 003 constraints (`chk_outer_not_alone`, `chk_outer_not_with_main`) were themselves wrong per an internal BRD Section 3.1 contradiction the independent architect review caught — they incorrectly rejected the valid Inner+Outer+Main combination explicitly permitted by rule 7 and Section 11.1. **Confirmed via the extended `scripts/test-led-constraints.mjs`** against both `assetmgmt` and `assetmgmt_dev` post-004: all 3 invalid combinations correctly rejected by the new single `chk_outer_requires_inner` constraint, and all 5 valid combinations (including Inner+Outer+Main) correctly accepted. Test rows cleaned up afterward. | Pass | |
| TC-11 | App Service → MySQL connectivity via Managed Identity (no password) | TC-05, TC-09 passed | 1. Grant Directory Readers to `uami-mysql-sportspres-assetmgmt` 2. Run `bootstrap-mysql-aad.mjs` to create AAD-mapped MySQL users for both App Service identities 3. Deploy app, hit `/api/health/db` | `{ ok: true }`, connecting as `app-sportspres-assetmgmt` / `app-sportspres-assetmgmt-dev` MySQL users with no password/connection string anywhere | **Confirmed live 2026-09-16**, after user had a tenant directory admin grant Directory Readers: `bootstrap-mysql-aad.mjs` ran successfully, and `https://app-sportspres-assetmgmt-dev.azurewebsites.net/api/health/db` now returns `{"ok":true}`. No password/connection string anywhere in code or App Service settings. | Pass | Prior to the Directory Readers grant, this failure correctly returned `Access denied for user 'app-sportspres-assetmgmt-dev'@'...'` — confirming the App Service was genuinely attempting the Managed-Identity AAD token connection all along (see D-05: that failure originally crashed the *entire app*, not just this health check, until fixed). |
| TC-12 | Super Admin default account seeded, login works end-to-end | TC-09, TC-11 passed | 1. `POST /api/auth/login` with `first_time_admin` / `Admin@123` against the live dev slot 2. `GET /api/auth/me` with returned cookie | Login succeeds, session cookie issued, `/me` returns `{ username: "first_time_admin", role: "SuperAdmin" }` | **Confirmed live** against `https://app-sportspres-assetmgmt-dev.azurewebsites.net`: both calls returned exactly this | Pass | |
| TC-13 | Super Admin change-password works and rejects wrong current password | TC-12 passed | 1. `POST /api/auth/change-password` with wrong `currentPassword` 2. Retry with correct current password and a valid new password 3. Log in with the new password to confirm it took effect 4. Restore the default password | Step 1 rejected (401); Step 2 succeeds (200); Step 3 succeeds; default password restored afterward | **Confirmed live** against the deployed dev slot: wrong password → 401 "Current password is incorrect"; correct password → 200; login with new password succeeded; password restored to `Admin@123` afterward so the documented default remains valid | Pass | |
| TC-14 | Administrator/Normal User Azure AD login | AAD app registration completed, App Roles assigned | 1. Sign in via "Sign In With Microsoft" 2. Confirm role read from the `roles` claim maps correctly to Administrator/NormalUser 3. Sign out | Session cookie issued with correct role; sign-out works | **Confirmed by user, live**: signed in via Azure AD as `vatsan@worldtabletennis.com` (assigned `Administrator` app role), landed on the authenticated dashboard shell, and signed out successfully. Three real bugs were found and fixed to get here — see workflow.md items 7–9 (wrong token type, missing `broadcastResponseToMainFrame()` call, CSP blocking the token exchange `fetch()`). | Pass | |
| TC-15 | Normal User cannot access Super Admin change-password endpoint | TC-14 | 1. As a Normal User session, `POST /api/auth/change-password` | 403 Forbidden | **Not run** — no second AAD test account assigned the `NormalUser` role exists to sign in with. Code enforces `req.user.role !== 'SuperAdmin' \|\| req.user.provider !== 'local'` in the route itself (`server/src/auth/routes.js`), independently of the `requireRole` middleware — reviewed, not live-exercised | Not run | Low risk given the check is a simple, directly-reviewable server-side condition, but flagging honestly rather than claiming a pass with no evidence. Can be closed out in Step 2+ once a second test account is available, or the user can create one now if they want it closed before sign-off. |
| TC-16 | Custom CSS actually renders on the deployed app (login, change-password, dashboard shell) | Deployed to `dev` | 1. Load `/login` in a browser 2. Inspect computed styles of `.wtt-cta`, `.wtt-tag` via `getComputedStyle` 3. Repeat for the dashboard shell after login | Buttons render WTT orange, role tag renders as a teal pill, card shows the orange accent border, dark themed form fields — not browser-default grey/white | **Failed on first check, then fixed.** Found via direct `getComputedStyle`/CSSOM inspection during the `ralph-loop` polish pass that computed `background-color` on `.wtt-cta` was `rgb(240,240,240)` (browser default) despite the correct rule existing in the deployed CSS file — traced to Angular's deferred-stylesheet pattern (`media="print" onload="this.media='all'"`) being silently blocked by helmet's default `script-src-attr: 'none'` CSP directive (inline event handler attributes). Fixed via `angular.json`'s `optimization.styles.inlineCritical: false`. Re-verified via `getComputedStyle` (now returns `rgb(255,107,0)`) and screenshots across all three components. | Pass | See D-07. This means **no custom styling had rendered anywhere in the app from Step 1's first deployment until this fix** — a significant finding caught by the polish pass, not by earlier functional testing (which never asserted on visual appearance). |

## Open defects / follow-ups

| ID | Linked test case | Description | Severity | Status |
|---|---|---|---|---|
| D-01 | TC-11 | ~~MySQL Managed-Identity connectivity blocked pending Directory Readers grant~~ | — | **Resolved 2026-09-16** — tenant directory admin granted Directory Readers, `bootstrap-mysql-aad.mjs` run, `/api/health/db` confirmed `{"ok":true}` live. TC-11 now Pass. |
| D-02 | TC-14, TC-15 | ~~Administrator/Normal User Azure AD login cannot be tested until WTT IT completes the app registration~~ | — | **Resolved 2026-09-16** — app registration completed, App Roles created and assigned, user confirmed live sign-in as Administrator. TC-14 now Pass. TC-15 (Normal User specifically) remains Not Run — no second test account exists yet, see D-04. |
| D-03 | TC-06 | ~~Storage connectivity had only been proven via the developer's identity~~ | — | **Resolved** — re-verified against the live `dev` slot using the App Service's own Managed Identity; TC-06 now Pass. |
| D-04 | TC-15 | Normal User role specifically has not been live-tested (only Administrator has a test account assigned) | Low — the server-side check is simple and directly reviewable (`role !== 'SuperAdmin'`), just not exercised end-to-end | Open — can be closed out once a second AAD test account with the `NormalUser` role exists; not blocking for Step 1 sign-off given the low risk and reviewable code |
| D-05 | TC-11 | Discovered while deploying: a failed `seedSuperAdmin()` call at startup (expected while D-01 is open) was crashing the *entire* Node process, taking down health checks, config, storage, and the frontend along with it — not just the DB-dependent path. | Medium — masked the real system state (looked like everything was down, when only MySQL was) | **Fixed** — `src/index.js` now catches the seed failure and continues starting the HTTP server; verified live: with MySQL still blocked, `/api/health`, `/api/config`, `/api/health/storage`, and the frontend all serve correctly while only `/api/health/db` reports the expected failure. |
| D-06 | TC-14 | Three real bugs found and fixed while getting Azure AD sign-in working: (1) frontend requested a Microsoft Graph access token instead of an ID token, wrong audience for the backend's validation; (2) the redirect/callback page never called `@azure/msal-browser`'s required `broadcastResponseToMainFrame()`, so the popup never completed (first attempted fix was based on a wrong assumption about the mechanism and didn't work either — the actual fix came from reading the installed library's source); (3) `helmet()`'s default CSP blocked the browser's `fetch()` to Azure AD's token endpoint, surfacing as a generic "Failed to fetch" with no useful explanation. | High during development (each blocked sign-in entirely) | **Fixed and verified live** — see workflow.md Step 1 items 6–9 for full detail on each. User confirmed successful sign-in, sign-out, and role assignment after all three fixes. |
| D-07 | TC-16 | **No custom CSS had rendered anywhere in the app since Step 1's first deployment.** Angular's default production build defers the main stylesheet via `<link media="print" onload="this.media='all'">`; the `onload` inline event handler was silently blocked by helmet's default `script-src-attr: 'none'` CSP directive, so the stylesheet stayed `media="print"` forever and never applied on screen. All pages were rendering with bare browser-default form control/button styling underneath only the small inline critical-CSS Angular extracts (which is why the dark page background still looked plausible at a glance, masking the rest). | High — every page was effectively unstyled in production the entire time, and no earlier functional test caught it since none asserted on visual appearance | **Fixed** — disabled Angular's critical-CSS-inlining optimization (`angular.json` `optimization.styles.inlineCritical: false`) rather than weakening CSP, so the stylesheet now loads as a plain blocking `<link>` with no inline handler. Verified via `getComputedStyle` inspection and screenshots that styling now correctly applies across login, change-password, and dashboard-shell. |
| D-08 | TC-10 | **Independent architect review caught two more real issues**, both fixed same day: (1) `httpsOnly` was actually `false` on both App Service slots despite the Security Checklist claiming "Pass" — Azure's real default is `false`, not `true` as the checklist assumed; (2) no brute-force protection existed on `/api/auth/login`/`/api/auth/change-password`, and adding `express-rate-limit` surfaced a genuinely separate bug — Azure's `X-Forwarded-For` includes the client's ephemeral port, which changes every request, so the default key generator never counted anything. Also: BRD Section 3.1 rule 2 ("Outer cannot be selected with Main") contradicts rule 7 and Section 11.1 — the original `003` migration's constraints incorrectly rejected the valid Inner+Outer+Main combination. | High (httpsOnly, LED constraint correctness) / Medium (rate limiting) | **All fixed and verified live.** `httpsOnly` set true on both slots (HTTP now redirects). Rate limiter fixed with a custom port-stripping `keyGenerator`; verified 9→0 countdown and `429` on the 11th attempt. LED constraint fixed via migration `004` after user confirmation — see TC-10. |

## Sign-off

| Role | Name | Date | Outcome |
|---|---|---|---|
| Independent Solution Architect review | (fresh subagent, no prior context) | 2026-09-16 | Rejected pending fix (httpsOnly), otherwise Approved with notes. Fix applied and re-verified same day — see D-08. |
| User (Vatsan) go-ahead | | | Pending |
