# QA Test Case Document — Step 4: Asset Upload – OVR Triggers (Backend)

## Header

| Field | Value |
|---|---|
| Application | Web |
| Step / Phase | Step 4 — Asset Upload – OVR Triggers |
| BRD reference(s) | Web BRD Sections 10, 15, 16, 17, 18, 18.1, 18.2, 19, 19.1, 20, 21, 22, 26, 28, 31 |
| Implementation Sequence reference | Web Implementation Sequence Step 4 (4.1–4.9) |
| Date | 2026-09-17 |
| Tested by | Claude Code (self-tested, backend via curl and frontend via live browser automation); independent Solution Architect review completed 2026-09-17 (Blocking issues found — 2 real bugs, plus 2 documentation/config-accuracy corrections — all fixed and re-verified live same day, see D-02 through D-06). |
| Environment | Azure: `app-sportspres-assetmgmt` `dev` slot, `mysql-sportspres-assetmgmt` (`assetmgmt_dev`), `sasportspresentation` storage account (`2026` container). Backend test cases (TC-01 through TC-24) used a dedicated test event (`9401 - QA Step4 OVR Triggers Test`, Table 1 with Inner+Outer+Main LED all enabled) created through the real `POST /api/events` API, exercised, and fully torn down (blobs + DB rows) afterward. Frontend test cases (TC-25 onward) used the pre-existing real sample event `1000 - Star contender Doha` (Table 1, Inner+Outer+Main all enabled, with real pre-existing OVR Trigger content) read-only — no uploads/deletes performed against it, per the project's rule not to modify existing storage content without explicit review. Real sample media from `references/For WTT/1_Sponsor_Ads/` (1920x1080 PNGs) and `references/For WTT/2_Home_Look/` (1920x1080 and 3840x2160 MP4s) used as backend test fixtures — not synthetic/placeholder files. |

## Summary

| Total cases | Passed | Failed | Blocked | Not yet run |
|---|---|---|---|---|
| 45 | 45 | 0 | 0 | 0 |

## Test cases

| ID | Description | Preconditions | Steps | Expected result | Actual result | Status | Notes |
|---|---|---|---|---|---|---|---|
| TC-01 | Event/table creation proactively creates empty Default Asset placeholders | New event created via `POST /api/events` with Table 1: Inner+Outer+Main all enabled | `az storage blob list` on the new event's folders | 0-byte `default.png`/`default.mp4` present in `Table 1/inner/`, `Table 1/outer/`, `Table 1/mainled/`; no `sponsorsequence.csv` under `mainled` (Section 7.1) | Confirmed live: all six placeholders present at exactly 0 bytes; `mainled` correctly has no `sponsorsequence.csv` | Pass | |
| TC-02 | `GET /:destination` returns the correct slot set per destination | TC-01 event | `GET .../ovr-triggers/inner`, `.../outer`, `.../main` | Inner/Outer: 8 shared trigger types + `home_look_surrounds` + All Sponsor Logo + 2 Default Asset slots (12 total). Main: 8 shared trigger types + `home_look_main` (not `home_look_surrounds`) + 2 Default Asset slots, no All Sponsor Logo (11 total) | Confirmed live: exact slot counts and `assetKey`s matched for all three destinations; `all_sponsor_logo.image` correctly absent from `main`'s slot list | Pass | |
| TC-03 | Home Look image (Inner/Outer): exact filename `HOME_Look.png` required, valid upload accepted | TC-01 event | Upload a real 1920x1080 PNG named `HOME_Look.png` to `inner/home_look_surrounds.image` | 200 `{ok:true}`; blob saved as `Table 1/inner/HOME_Look.png` | Confirmed live | Pass | |
| TC-04 | Home Look image upload also produces the `additionalCopy` into `default.png` (Web BRD Section 19/20) | TC-03 done | `az storage blob list` on `Table 1/inner/` | `default.png` now has real content (same size as the uploaded `HOME_Look.png`), no longer the 0-byte placeholder | Confirmed live: `default.png` size matched the uploaded file's size exactly | Pass | |
| TC-05 | Home Look video (Inner/Outer): any filename accepted, always renamed to `Home Look.mp4` | TC-01 event | Upload a real 1920x1080 MP4 with an arbitrary filename to `inner/home_look_surrounds.video` | 200 `{ok:true}`; blob saved as `Table 1/inner/Home Look.mp4` regardless of the uploaded filename | Confirmed live | Pass | |
| TC-06 | Standard trigger (Time Out) with correct exact filename accepted | TC-01 event | Upload a real 1920x1080 MP4 named `TO.mp4` to `inner/time_out.video` | 200 `{ok:true}` | Confirmed live | Pass | |
| TC-07 | Standard trigger with wrong filename rejected (Web BRD Section 17) | TC-01 event | Upload a real 1920x1080 MP4 named `wrongname.mp4` to `inner/time_out.video` | 400: `Filename must be exactly "TO.mp4" (got "wrongname.mp4")` | Confirmed live | Pass | |
| TC-08 | Standard trigger with correct filename but wrong resolution rejected | TC-01 event | Upload a real 3840x2160 MP4 named `TO.mp4` to `inner/time_out.video` | 400: `Video must be 1920x1080 (got 3840x2160)` | Confirmed live | Pass | |
| TC-09 | Main LED resolution validation reads `event_tables`' `main_resolution_*` columns (3840x2160 default), independently of Inner/Outer's 1920x1080 | TC-01 event | Upload the same 1920x1080 `TO.mp4` to `main/time_out.video` (wrong for Main); then upload a real 3840x2160 file named `TO.mp4` to the same slot | First: 400 `Video must be 3840x2160 (got 1920x1080)`. Second: 200 `{ok:true}` | Confirmed live both directions | Pass | Reuses the same `resolutionForDestination()`/`event_tables` mechanism Step 3 built and this step's kickoff explicitly extended to Main LED. |
| TC-10 | Main LED Home Look (`home_look_main`) accepts any filename, validated against Main's own 3840x2160 resolution | TC-01 event | Upload a real 3840x2160 MP4 with an arbitrary filename to `main/home_look_main.video`; separately upload a real 1920x1080 MP4 to the same slot | First: 200 `{ok:true}`, saved as `Table 1/mainled/Home Look.mp4`. Second: 400 `Video must be 3840x2160 (got 1920x1080)` | Confirmed live both directions | Pass | |
| TC-11 | Winning Moment fallback: `winner.mp4` absent when no default and no real upload exist | TC-01 event | `GET .../ovr-triggers/inner`, inspect the `winning_moment.video` slot | `exists: false` | Confirmed live | Pass | |
| TC-12 | Winning Moment fallback is physically enforced on `default.mp4` upload (Web BRD Section 18.1) | TC-11 state | Upload a real 1920x1080 MP4 to `inner/default.video`; re-check the `winning_moment.video` slot | `winner.mp4` now exists (copied from `default.mp4`, same byte size) | Confirmed live: slot flips to `exists: true`; `az storage blob list` confirmed `winner.mp4` present with the same size as `default.mp4` | Pass | |
| TC-13 | A real (non-empty) `winner.mp4` upload is never silently overwritten by the fallback | TC-12 state | Upload a distinct real MP4 as `winner.mp4` (exact filename) to `inner/winning_moment.video` | 200 `{ok:true}`; blob content is the newly uploaded file, not touched by any fallback logic | Confirmed live | Pass | |
| TC-14 | Deleting a real `winner.mp4` restores the fallback copy from `default.mp4` (still present) | TC-13 state | `DELETE .../ovr-triggers/inner/winning_moment.video` | Delete succeeds; immediately after, `winning_moment.video`'s slot shows `exists: true` again (re-copied from `default.mp4`) | Confirmed live: `GET` immediately after the delete shows `exists: true` | Pass | |
| TC-15 | The fallback's own auto-copy is recorded in the change log (Web BRD Section 24) | TC-14 repeated after the D-01 fix | Delete `winner.mp4` again; download `_ledassetschangelog.csv` | Two adjacent rows: a `Deleted` entry for the delete itself, and a `New` entry for `Table 1/Inner/winner.mp4` for the fallback's re-copy | **Failed once** (fallback copy wasn't logged at all — see D-01), **then fixed**. Confirmed live after the fix: both rows present in that order (`Deleted` at 09:59:27, `New` at 09:59:32) | Pass | See D-01. |
| TC-16 | Champion Winning Moment has no fallback relationship with Winning Moment/Default in either direction (Web BRD Section 26) | TC-12 state (winner.mp4 exists via fallback) | Upload `champion_winner.mp4` (exact filename) to `inner/champion_winning_moment.video`; then delete it | Upload succeeds independently; deleting it does not affect `winning_moment.video`'s slot at all | Confirmed live: `winning_moment.video` still `exists: true` immediately after `champion_winning_moment.video` was deleted | Pass | |
| TC-17 | All Sponsor Logo: exact filename `GPMP.png` required, saves into **both** Inner and Outer as `all-adv.png`, never Main LED (Web BRD Section 22) | TC-01 event | Upload a real 1920x1080 PNG named `GPMP.png` to `inner/all_sponsor_logo.image` | 200 `{ok:true}`; `all-adv.png` present in **both** `Table 1/inner/` and `Table 1/outer/`, same size in both | Confirmed live via `az storage blob list` on both folders | Pass | |
| TC-18 | All Sponsor Logo wrong filename rejected (exact match, not "contains" — user's explicit decision) | TC-01 event | Upload the same PNG named `GPMP_v2.png` | 400: `Filename must be exactly "GPMP.png" (got "GPMP_v2.png")` | Confirmed live | Pass | |
| TC-19 | All Sponsor Logo is not offered as a Main LED asset at all | TC-01 event | `POST .../main/all_sponsor_logo.image/upload` with a valid `GPMP.png` | 404: `Unknown asset type for this destination` | Confirmed live | Pass | |
| TC-20 | RPI Home Look (event-level, no table dimension) accepts a "contains" filename rule and validates against the fixed 1920x1080 Surrounds default | New event, no table parameter used at all | `POST /api/events/9401/ovr-triggers/rpi/upload` with a real 1920x1080 PNG whose filename contains `16x9_1080 STILL` | 200 `{ok:true}`; `GET .../rpi` returns `{exists:true}` | Confirmed live | Pass | RPI has no `event_tables` resolution column — validated against a documented fixed constant, not literal BRD text (see workflow.md). |
| TC-21 | OVR Trigger Preview streams the exact saved file with the correct content-type (Web BRD Section 28) | TC-03 done (Home Look image uploaded) | `GET .../inner/home_look_surrounds.image/preview` | 200, `Content-Type: image/png`, `Content-Length` exactly equal to the uploaded file's byte size | Confirmed live: `Content-Length: 47618` matched the uploaded PNG's exact size, downloaded bytes identical | Pass | |
| TC-22 | Unauthenticated requests rejected on OVR Trigger routes | — | `GET .../ovr-triggers/inner` with no session cookie | 401 | Confirmed live | Pass | Structurally guaranteed via `ovrTriggersRouter.use(requireSession, ...)`/`rpiRouter.use(requireSession, ...)`, same pattern as Step 3. |
| TC-23 | Change log entries use consistent Inner/Outer/MainLED/RPI casing across all destinations (Web BRD Section 24) | TC-01 through TC-20 done | Download `_ledassetschangelog.csv` | Every row's `filename` uses `Table N/Inner/...`, `Table N/Outer/...`, `Table N/MainLED/...`, or `RPI/...` consistently, matching the shared `changeLogPath()` helper (same helper family as Step 3) | Confirmed live: all 15 rows correctly cased, no lowercase inconsistencies | Pass | |
| TC-24 | `npm audit` clean; no new dependencies introduced this step | — | `npm audit` in `server/` | 0 vulnerabilities | Confirmed: 0 vulnerabilities. No new packages added — this step reuses `multer`, `pngMetadata.js`, `mp4Metadata.js`, `sponsorAdValidation.js`'s exported `IMAGE_SPEC` from Step 3 | Pass | |

| TC-25 | OVR Match Triggers tab is enabled (no longer the Step 3 disabled placeholder) | Signed in as `first_time_admin`, on the Asset Upload page | Click the "OVR Match Triggers" tab | Tab becomes active (orange underline), content renders — not disabled/greyed out | Confirmed live via browser automation | Pass | |
| TC-26 | Per-destination OVR Trigger sections render the correct trigger set and match real backend/blob state exactly | Real event `1000 - Star contender Doha`, Table 1 (Inner+Outer+Main all enabled, with real pre-existing OVR Trigger content) | Select the event/table, view the OVR Match Triggers tab | Three columns (Inner Surround, Outer Surround, Main LED); Main LED shows `home_look_main` (not `home_look_surrounds`) and correctly has no All Sponsor Logo section; every slot's "Uploaded"/"Not uploaded" tag matches the real blob listing gathered independently via `az storage blob list` before this test | Confirmed live: every one of ~30 rendered slots across all three columns matched the real storage state exactly (e.g. Inner Time Out/Game Point/Match Point/Championship Point/Winning Moment/All Sponsor Logo all correctly "Uploaded"; Main LED's Home Look/Time Out/Water Break/etc. all correctly "Not uploaded" since that folder only had a `keepalive.txt`) | Pass | |
| TC-27 | RPI Home Look section (event-level) renders independently of table selection | Same event | View the OVR Match Triggers tab | "RPI Home Look" section appears above the three destination columns, labeled "Event-level asset — applies to the whole event, not a specific table," correctly showing "Not uploaded" (matches the real `rpi/` folder containing only `keepalive.txt`) | Confirmed live | Pass | |
| TC-28 | Default Assets sections render on both the Sponsor Ads tab (Inner/Outer) and the OVR Match Triggers tab (Main LED), reflecting real per-destination state | Same event | View "Default Assets — Inner Surround"/"Outer Surround" on the Sponsor Ads tab; view "Default Assets — Main LED" on the OVR Match Triggers tab | Inner/Outer's Default Image correctly shows "Uploaded" (matches the real `default.png` present in both folders from earlier real usage); all three destinations' Default Video correctly shows "Not uploaded" (matches — no `default.mp4` had been uploaded to this pre-existing event) | Confirmed live | Pass | |
| TC-29 | Image preview renders the real file inline | Same event, Inner's "All Sponsor Logo" slot (`all-adv.png`, real uploaded content) | Click "Preview" | Button toggles to "Hide Preview"; an `<img>` showing the real sponsor-logo montage image renders inline, byte-identical to the actual blob (confirmed visually — a real multi-sponsor logo composite, not a broken-image icon) | Confirmed live | Pass | |
| TC-30 | Video preview renders the real file inline with working native controls | Same event, Inner's "Match Point" slot (`MATCH_POINT.mp4`, real uploaded content) | Click "Preview" | Button toggles to "Hide Preview"; a `<video controls>` element renders inline with a visible play button and scrubber, not a broken/blank player | Confirmed live | Pass | |
| TC-31 | No JavaScript console errors during navigation, tab switching, or preview toggling | Same session | `read_console_messages` after the above interactions | No error-level console output | Confirmed: no console messages captured (tracking active throughout) | Pass | |

| TC-32 | All Sponsor Logo's dual-folder save respects the sibling destination's own LED enablement — never writes into a disabled/non-existent folder (independent architect review finding, D-02) | Event with Table 1: Inner enabled, Outer disabled | `POST .../inner/all_sponsor_logo.image/upload` with a valid `GPMP.png` | `all-adv.png` saved to `Table 1/inner/` only; no `outer` folder is created at all | **Failed once** (created an orphaned `outer` folder with `all-adv.png` in it, on a table where Outer LED is disabled — live-confirmed by the independent review via `az storage blob list`), **then fixed**. Re-verified live after the fix: `az storage blob list` on the same table shows no `outer` prefix exists | Pass | See D-02. |
| TC-33 | Home Look's `additionalCopy` write to `default.png` is change-logged on upload, and is no longer destructively cascade-deleted when Home Look itself is deleted (independent architect review finding, D-03) | Table with Home Look uploaded (setting `default.png`) | Upload a Home Look image; download the change log; then delete the Home Look image; check `default.png` still exists | Upload: change log has two adjacent `New` rows (`HOME_Look`/`HOME_Look.png` and `default.png`). Delete: only the Home Look file is removed; `default.png` is untouched (still present, same byte size as before) | **Failed once** (upload's `default.png` write was completely unlogged; delete additionally cascade-deleted `default.png` with zero audit trail — live-confirmed by the independent review via before/after blob listings and the change log), **then fixed** — see D-03 for the design reasoning (the cascade-delete was removed entirely, not just logged, since `default.png` is also the independently-manageable Default Assets slot). Re-verified live: change log now shows the `default.png` `New` entry on upload; `default.png` survives a Home Look delete completely intact | Pass | |
| TC-34 | Main LED Home Look's image is saved under the exact filename the real `asset_management_templates.json` specifies (`HOME_Look`, no extension) — not a BRD-prose guess (independent architect review finding, D-04) | Table with Main LED enabled | Upload a real 3840x2160 PNG (any filename) to `main/home_look_main.image` | Blob saved as `Table N/mainled/HOME_Look` (no `.png` extension) | **Failed once** (saved as `HOME_Look.png`, since `templateConfig.js` was derived from BRD prose without realizing the actual JSON config file exists in the repo, at `references/0forimplementation/web/asset_management_templates.json`), **then fixed**. Re-verified live: `az storage blob list` shows `Table 1/mainled/HOME_Look` with no extension | Pass | See D-04. |
| TC-35 | Winning Moment's `usingFallback` status flag actually reports `true` when the slot is running on an inherited default, not just `exists: true` (independent architect review finding, D-05) | `winner.mp4` deleted with a real `default.mp4` present | `GET .../inner`, inspect `winning_moment.video` immediately after the fallback copy | `exists: true`, `usingFallback: true` | **Failed** (the flag's original logic only checked `!status.exists`, which is never true once the fallback is physically enforced — the flag could never fire), **then fixed** by tagging the fallback copy with blob metadata and reading it back. A second, self-introduced bug surfaced during the fix (see D-06) and was fixed in the same pass before this test was marked Pass. Re-verified live: `usingFallback: true` immediately after a fallback copy; uploading a real `winner.mp4` afterward correctly flips it back to `false` while `exists` stays `true` | Pass | See D-05, D-06. |
| TC-36 | Uploading a real `winner.mp4` after a fallback copy fully clears the fallback tag (no stale "Using Default" status survives a real upload) | TC-35 state (fallback copy active) | Upload a real, distinct `winner.mp4` | `usingFallback` flips to `false`; blob metadata no longer contains the fallback tag | Confirmed live via direct blob inspection (`az storage blob show`) and the status endpoint | Pass | A block blob's `upload()` fully replaces the blob including metadata, so this requires no explicit "unset" logic. |
| TC-37 | `asset_management_templates.json` cross-check against `templateConfig.js`, entry by entry | — | Read `references/0forimplementation/web/asset_management_templates.json` directly and compare every trigger type's filename rule/storage filename/destinations against `templateConfig.js` | All entries match except the one found in D-04 | Confirmed: Time Out, Water Break, Game Point, Match Point, Championship Point, Winning Moment (including its fallback config), Champion Winning Moment, Home Look (Inner/Outer), and RPI Home Look all match exactly; only Main LED Home Look's image `storageFilename` diverged (D-04) | Pass | This file's existence was missed during Step 4's original kickoff research (which only searched the Desktop app codebase and the `templates` blob container, not the `references/` subtree) — corrected in `templateConfig.js`'s header comment and `workflow.md`. |

| TC-38 | Page width uses most of a wide viewport instead of a fixed 1080px card (user feedback + independent UI/UX review) | 1920px browser window | Load the Asset Upload page | `.wtt-upload-card`'s rendered width is ~1600px (via `min(94vw, 1600px)`), not the old fixed 1080px; no horizontal page overflow | Confirmed live via direct DOM measurement (`getBoundingClientRect()`): `innerWidth: 1920`, `cardWidth: 1600`, `scrollWidth: 1905` (no overflow) | Pass | |
| TC-39 | Sponsor Ads file table no longer truncates filenames | Real event `1000`/Table 1, files with longer names (`(ENG) Frankfurt.png`, `CHMP_POINT.mp4`, etc.) | View the Sponsor Ads tab, both Inner and Outer tables | Every filename renders in full, no ellipsis/truncation | Confirmed live via screenshot — all filenames fully visible in both columns | Pass | |
| TC-40 | Retrieve Video Duration is now a compact icon-only button with a tooltip, not a 300px text button | Same event, a video row | View a video row's Duration cell | A small (28px) icon button appears next to the duration input, with `title`/`aria-label` "Retrieve video duration"; clicking it still works | Confirmed live: icon button renders correctly on every video row in both Inner and Outer tables | Pass | |
| TC-41 | OVR Trigger / RPI / Default Assets slots render as compact, self-contained cards with a differentiating thumbnail | Same event/table, OVR Match Triggers tab | View RPI Home Look, all three destination columns, and Default Assets — Main LED | Each slot is a bordered card (not a bare full-width row); uploaded PNG slots show a real image-crop thumbnail, uploaded MP4 slots show a video-camera glyph, empty slots show a dashed placeholder glyph; action buttons render as secondary (transparent/bordered), not solid orange | Confirmed live via screenshots across RPI, all three destination columns, and Default Assets — Main LED | Pass | See D-07 below — the secondary-button styling was a real, independently-found bug on top of the layout fix. |
| TC-42 | Destination columns grid degrades gracefully instead of squeezing evenly thin | Table with Inner+Outer+Main all enabled | View the OVR Match Triggers tab's three-column layout | Columns use `grid-template-columns: repeat(auto-fit, minmax(320px, 1fr))` - three real columns render side by side at the current page width, each comfortably wider than the 320px minimum | Confirmed live: three columns rendered correctly at ~500px each within the 1600px page | Pass | |
| TC-43 | Preview works identically for Default Image and Default Video slots (user's complaint checked and found not accurate as stated) | Default Assets section with an uploaded Default Image | Click Preview on the Default Image slot | Real image renders inline | Confirmed live (via the Home Look / Main LED Default Image slot, which was in the Uploaded state on the real test event) — the same component and gating logic serves Default Video identically; no Default Video slot happened to be uploaded on the live event to demo directly, but the code path is shared and was already verified for video kind elsewhere (TC-30) | Pass | Not a bug — see D-07's write-up in workflow.md for the full explanation. |
| TC-44 | New "Asset Rules" view-only settings modal | Any authenticated session | Click "Asset Rules" in the Asset Upload page header | Modal opens showing Sponsor Ads rules, the full OVR Trigger table (10 rows), All Sponsor Logo, RPI Home Look, and Default Assets sections, all matching the actual backend config exactly; closes via the × button or Escape | Confirmed live: all sections rendered correctly and matched `templateConfig.js`/`ALL_SPONSOR_LOGO`/`RPI_RESOLUTION` exactly (single-sourced via `GET /api/asset-rules`); Escape correctly closed it | Pass | |
| TC-45 | No JavaScript console errors introduced by the redesign | Same session | `read_console_messages` after navigating both tabs, opening the modal, toggling previews | No error-level output | Confirmed: no console messages captured | Pass | |

## Open defects / follow-ups

| ID | Linked test case | Description | Severity | Status |
|---|---|---|---|---|
| D-01 | TC-15 | `applyWinningMomentFallback()`'s auto-copy of `default.mp4` into the `winner.mp4` slot silently changed real file content on disk (both on upload of `default.mp4` and on delete of `winner.mp4`) without writing any change-log entry — the same class of audit-completeness gap the independent architect review flagged for `sponsorsequence.csv`'s implicit rewrites in Step 3. Caught via live testing: confirmed the fallback copy physically happened (`GET` status flipped to `exists:true`, blob size matched `default.mp4`) but the change log had no corresponding row. | Low — audit-completeness gap (Web BRD Section 24), not a data-integrity or security issue; the fallback logic itself was already correct | **Fixed**: `applyWinningMomentFallback()` now takes the acting username and logs its own `New` change-log entry whenever it actually performs the copy (never when it's a no-op because `winner.mp4` already has real content, avoiding false entries). Re-verified live (TC-15). |
| D-02 | TC-32 | Independent architect review: `resolveAssetSlot`'s `saveToBothInnerAndOuter` (used by All Sponsor Logo) unconditionally wrote into both `inner` and `outer` regardless of whether the *sibling* destination was actually enabled for that table — `requireValidDestination` only gates the URL's own `:destination` param, not the "also save to" side. Live-confirmed by the reviewer: uploading to `inner` on a table with Outer disabled created a brand-new, orphaned `outer` folder (with its own `all-adv.png`) that the app could then never manage again (`GET`/`DELETE` on `outer` correctly 400 since Outer is disabled — only the write path was missing the check). | Medium — real data-integrity gap; created storage structure inconsistent with the table's own configuration, unrecoverable through the app itself | **Fixed**: both the upload and delete handlers now filter `destinationsToSave`/`destinationsToDelete` against `req.eventContext.table`'s own `innerLed`/`outerLed` flags before looping. Re-verified live (TC-32). |
| D-03 | TC-33 | Independent architect review: Home Look's `additionalCopy` write to `default.png` had no change-log entry on upload, and — more seriously — the delete handler unconditionally deleted `default.png` whenever the Home Look image itself was deleted, with zero audit trail. Since `default.png` is also the independently-manageable "Default Assets" slot (a first-class feature per this step's own kickoff decision 3), this meant deleting an unrelated Home Look image could silently destroy a Default Image a user had separately, deliberately uploaded afterward. | Medium-High — real, silent data loss with no audit trail; the BRD text `additionalCopy` is based on ("must also be copied and renamed as: default.png") only describes the copy-on-upload behavior, nothing about a delete cascading | **Fixed with a design change, not just a log entry**: the cascade-delete of `additionalCopy` was removed entirely (upload still sets `default.png`; delete no longer touches it), since a one-directional relationship is both safer and more consistent with the Winning Moment fallback's own one-directional design elsewhere in this file. The upload side's `default.png` write is now change-logged. Re-verified live (TC-33). |
| D-04 | TC-34, TC-37 | Independent architect review: `templateConfig.js`'s header comment claimed `asset_management_templates.json` "does not exist anywhere" — it does, at `references/0forimplementation/web/asset_management_templates.json`, missed during the original kickoff research. Cross-checking against it found one real divergence: Main LED Home Look's image should save as `HOME_Look` (no extension, matching BRD Section 20's literal text too), but `templateConfig.js` had `HOME_Look.png`. | Low-Medium — undocumented deviation from both the real config file and the BRD's own literal text; low practical impact (an internal storage filename) but a real accuracy gap in a step that otherwise prides itself on tracing every filename to source | **Fixed**: `templateConfig.js` corrected to `storageFilename: "HOME_Look"`; header comment corrected to acknowledge the file's real location and that this module is now reconciled against it. Re-verified live (TC-34). Also see workflow.md for the related correction to this step's Section 18 blob-storage-location deviation note (the config file is repo-local, not blob-storage-hosted, which BRD Section 18 does actually call for — a real, now-acknowledged deviation, not the "no equivalent requirement" claim the original kickoff research made). |
| D-05 | TC-35 | Independent architect review: the `usingFallback` status flag (and its "Using Default" frontend tag) was effectively unreachable dead code. Its logic only set `usingFallback: true` when `!status.exists` — but the Winning Moment fallback is *physically enforced* (`applyWinningMomentFallback()` performs a real synchronous copy), so by the time any status check ran, the file already existed and that branch could never fire. Operationally, staff had no way to tell from the UI whether "Winning Moment: Uploaded" meant a real upload or an inherited default. | Low — UX/observability gap, not a security or data-integrity issue; the fallback enforcement itself was already correct | **Fixed**: `applyWinningMomentFallback()` now tags the copy with blob metadata (`{fallbackcopy: "true"}`) via new optional `metadata` parameters on `uploadOvrTriggerFile`/`copyOvrTriggerFileWithinFolder`; the status endpoint reads it back to set `usingFallback`. A real upload of `winner.mp4` fully overwrites the blob (and its metadata), so the tag self-clears with no extra bookkeeping. Re-verified live (TC-35, TC-36). |
| D-06 | TC-35 | Self-introduced during the D-05 fix, caught before sign-off: the fallback-copy metadata tag was first written with a camelCase key (`fallbackCopy`), but the Azure Storage Blob SDK lowercases metadata key names when parsing the response (`x-ms-meta-*` header names are case-insensitive and Node/the SDK normalize them), so `status.metadata?.fallbackCopy` never matched the actual lowercased key on read - reproducing D-05's exact symptom (`usingFallback` still always `false`) despite the write side working correctly (confirmed via a direct SDK debug script showing the blob really did have the metadata, just under a different casing than the read side was checking). | Low — caught via live testing before this step's sign-off, never reached the QA/Security docs' "Pass" claims in a broken state | **Fixed**: both the write (`{ fallbackcopy: "true" }`) and read (`status.metadata?.fallbackcopy`) now consistently use the lowercase key. Re-verified live (TC-35). |
| D-07 | TC-41 | Found while implementing the independent UI/UX design review's redesign spec, not by the review itself: `.wtt-cta--secondary` and `.wtt-remove-table` were never defined in `ovr-slot-row.component.scss`. Angular's view encapsulation scopes component styles per-component, so a class only styled in a *different* component's stylesheet (both were defined in `sponsor-destination-section.component.scss`, not here) silently gets zero styling — every Preview/Replace/Upload button across the entire OVR Match Triggers tab, RPI section, and both Default Assets sections had been rendering as a solid-orange *primary* button this whole session, with the delete (×) button rendering with default browser button styling instead of the intended borderless red-text style. | Medium — a real, independently-found contributor to the "too many buttons" complaint (no visual hierarchy between primary/secondary actions at all), present since this component was first built, undetected until literally inspecting the rendered page for the redesign work | **Fixed**: both classes now defined locally in `ovr-slot-row.component.scss`, matching the pattern already used in `sponsor-destination-section.component.scss`. Re-verified live (TC-41) — buttons now render as transparent/bordered secondary buttons. |

## Not yet in scope for this document

- A live two-request concurrency race on the change log during OVR Trigger writes was not forced, consistent with Step 3's TC-23 precedent — `appendChangeLogEntry()`'s existing ETag-conditional-write-with-retry (built in Step 3, reused unchanged here) was verified via code review rather than by forcing an actual race.
- Frontend upload/delete interactions (as opposed to read-only status/preview rendering) were not exercised live in a browser against a real event, to avoid modifying the pre-existing real sample event's storage content without review (per the project's standing rule). The underlying upload/delete endpoints themselves are fully verified live via curl throughout this document (including the D-02 through D-06 fixes); only the literal "click Upload/Delete in the browser and see it succeed" interaction is unverified, consistent with Step 3's TC-21 precedent for the same reason.

## Sign-off

| Role | Name | Date | Outcome |
|---|---|---|---|
| Independent Solution Architect review | (fresh subagent, no prior context) | 2026-09-17 | Approved with notes — found 2 real blocking bugs (D-02, D-03) and 2 documentation/config-accuracy corrections (D-04, and the Section 18 blob-storage-location deviation note), plus verified everything else (fallback isolation, RPI, resolution enforcement, All Sponsor Logo's Main LED exclusion, `npm audit`, `ng build`) both via code review and its own independent live testing. All findings fixed same day; D-05's fix surfaced a second self-contained bug (D-06), also fixed and re-verified same day. |
| User (Vatsan) go-ahead | | 2026-09-17 | Approved — "Commit Step 4" (opted to test Steps 4/5/6 together rather than a separate round for Step 4). |
