# QA Test Case Document — Step 5/6: Event Configuration (Add/Change/Delete), Dashboard/Event List, Asset Management Settings Edit, User Management

## Header

| Field | Value |
|---|---|
| Application | Web |
| Step / Phase | Step 5 (Event Configuration – Add/Change/Delete) combined with Step 6 (Dashboard/Event List), per the user's explicit instruction 2026-09-17. Also includes several features added to this step's scope by explicit user request during review: a structured (non-JSON-free-text) Asset Management Settings editor, a new User Management screen with Azure AD directory search (including B2B guest accounts), an Archive action, and a per-user Favorites list. |
| BRD reference(s) | Web BRD Sections 8, 9, 3.1, 18, 25 |
| Implementation Sequence reference | Web Implementation Sequence Steps 5, 6 |
| Date | 2026-09-18, revised/extended 2026-09-19 |
| Tested by | Claude Code (self-tested: backend via curl against the live `dev` slot, frontend via live browser automation with Claude in Chrome, plus real live usage by the actual human user during the session). An independent UI/UX review by a fresh subagent was run 2026-09-19 - see workflow.md for its findings. No independent Solution Architect (backend/logic) review has been run on this step yet. |
| Environment | Azure: `app-sportspres-assetmgmt` `dev` slot, `mysql-sportspres-assetmgmt` (`assetmgmt_dev` and, for migrations, `assetmgmt`), `sasportspresentation` storage account (`2026` and `2027` containers). Backend/frontend test cases used dedicated disposable test events created through the real UI/API (`9501`/`9502` — same event, renamed then archived mid-testing; the pre-existing `9101` QA test event from Step 3 was also reused read-mostly). Real Azure AD users (`romal@worldtabletennis.com` and the guest account `vatsan@ittf.com`) were added via User Management to prove the Microsoft Graph integration end-to-end - `romal@worldtabletennis.com` was removed again after its own test; `vatsan@ittf.com` and its NormalUser role were deliberately left in place, since the user requested it specifically to continue testing the Normal User experience themselves. No other real user's access was changed by this testing. |

## Summary

| Total cases | Passed | Failed | Blocked | Not yet run |
|---|---|---|---|---|
| 40 | 38 | 0 | 0 | 2 |

## Test cases

| ID | Description | Preconditions | Steps | Expected result | Actual result | Status | Notes |
|---|---|---|---|---|---|---|---|
| TC-01 | `PUT /api/events/:eventId` requires an authenticated session | — | `PUT /api/events/9999` with no session cookie | 401 `Not authenticated` | Confirmed live | Pass | Also confirmed the route exists at all (a transient 404 seen immediately after one `az webapp restart` turned out to be the restart window itself, not a routing bug — resolved to 401 within ~30s once the slot finished cycling). |
| TC-02 | Table deletion always requires confirmation, even with zero real files (Web BRD Section 9, unconditional) | Test event `9501` created via `POST /api/events` with Table 1 + Table 2, no assets uploaded to either | Edit the event: remove Table 2, submit without `confirmed` | `409 { confirmations: [{ type: "table-delete", tableNumber: 2, fileCount: 0, message: "Deleting Table 2 will permanently remove its folder and 0 file(s) in it." }] }` | Confirmed live via browser: the modal correctly displayed this exact message even though the table had no real uploaded files | Pass | |
| TC-03 | LED-destination disable only requires confirmation when real files exist (Web BRD Section 8, conditional) | Event `9101` (Step 3's QA test event), Table 1 with real Sponsor Ad files uploaded to both Inner and Outer | Edit the event: uncheck Outer LED on Table 1, submit without `confirmed` | `409` with a `destination-disable` confirmation whose message names the real file count | Confirmed live: modal showed "Disabling OUTER LED on Table 1 will permanently remove 2 file(s)" — matched Step 3's own QA record of what was uploaded there. Cancelled rather than applying it, leaving `9101` untouched. | Pass | |
| TC-04 | Event ID/Name/Year rename does **not** require confirmation when the event has no real (non-scaffolding) files yet | Freshly-created test event `9501`, no assets uploaded | Edit the event: change Event ID, Event Name, and Year simultaneously, submit without `confirmed` | No `409` — the edit applies directly, since `listRealFiles()` found nothing but scaffolding under the whole event prefix | Confirmed live: the confirmation modal showed only the `table-delete` item (TC-02, same request), no `rename` item, even though ID/Name/Year all changed at once | Pass | |
| TC-05 | Event ID rename cascades to `event_tables` via migration 005's `ON UPDATE CASCADE`, inside the same transaction | TC-04 request confirmed and applied (`9501` → `9502`) | `GET /api/events/9501` (old ID); `GET /api/events/9502` (new ID) | Old ID: 404. New ID: 200, with the correct name/year/table data | Confirmed live: `9501` → `{"error":"Event not found"}` (404); `9502` → correct full event detail (Table 1 only, since Table 2 was deleted in the same request) | Pass | Required migration 005, applied to both `assetmgmt` and `assetmgmt_dev` before this test — see workflow.md. |
| TC-06 | Year change moves the event's storage to a different per-year blob container (not just a prefix rename within one container) | Same rename (`9501`/2026 → `9502`/2027) | `az storage blob list` on both the `2026` and `2027` containers | `2026` container has no `9501` prefix left at all; `2027` container has the full folder tree under `9502 - QA Step5 Rename Test Renamed/` | Confirmed live: `2026` prefix listing empty; `2027` listing showed all 8 expected blobs (event-level `keepalive.txt`/changelog, `rpi/keepalive.txt`, `Table 1/keepalive.txt` + `Table 1/inner/*`) | Pass | |
| TC-07 | Table deletion removes both the DB row and the storage folder subtree | Same request as TC-02/04/05/06 (Table 2 removed) | `GET /api/events/9502`; `az storage blob list` under the new prefix | Table 2 absent from the DB response; no `Table 2/` blobs anywhere under the new prefix | Confirmed live: `GET` shows only `tableNumber: 1`; blob listing has zero `Table 2` entries | Pass | |
| TC-08 | Table addition creates a full storage folder structure (same shape as event creation) | `9502`, Table 1 only | Edit the event: add Table 2 back (Inner LED only), save (no confirmation needed — purely additive) | `GET /api/events/9502` shows both tables; storage has a full `Table 2/` tree (`keepalive.txt`, `inner/keepalive.txt`, `inner/sponsorsequence.csv`, `inner/default.png`, `inner/default.mp4`) | Confirmed live via both the API and `az storage blob list` | Pass | |
| TC-09 | `GET /api/events` (the Dashboard/Event List) includes each event's table/LED summary | Any Active events exist | `GET /api/events` with a valid session | Each row has a `tables` array (`tableNumber`/`innerLed`/`outerLed`/`mainLed`), in addition to the existing `eventId`/`eventName`/`year`/`status` fields Step 3's picker already used | Confirmed live via the Dashboard UI, which renders a correct per-event LED summary (e.g. "Table 1 (Inner/Outer/Main), Table 2 (Inner)") for all three real events | Pass | Purely additive to the existing endpoint — Step 3's Asset Upload picker, unaffected, still works (TC-27). |
| TC-10 | `GET`/`PUT /api/asset-rules/raw` require Admin/SuperAdmin (`requireRole`) | — | `GET /api/asset-rules/raw` with no session | 401 | Confirmed live | Pass | Role-level rejection (403 for an authenticated NormalUser) was not exercised live this session — there is no NormalUser test account available (Administrator/NormalUser are Azure AD-authenticated real people; see TC-19/TC-20). `requireRole()` is the same helper already live-verified for other routes in Steps 1–4. |
| TC-11 | Asset Rules structured editor round-trips the full config with no data loss | Live seeded config (10 OVR Trigger types + All Sponsor Logo) | Load `/asset-rules`, click Save without changing anything, re-fetch `GET /api/asset-rules/raw` | All 10 trigger types and All Sponsor Logo present with identical field values (including optional fields like `additionalCopy`/`fallbackFrom`/`optional`) | Confirmed live: re-fetched config has `ovrTriggerTypes.length === 10`; spot-checked `winning_moment`'s `fallbackFrom: "default.mp4"`, `champion_winning_moment`'s `optional: true`, and `allSponsorLogo`'s exact fields — all preserved correctly | Pass | |
| TC-12 | Server rejects a malformed config with a clear, field-specific error (defense in depth — the structured editor itself can't produce most of these) | — | `PUT /api/asset-rules/raw` with a trigger missing `label`/`destinations`/`files` and an empty `allSponsorLogo` | 400 with a `details` array naming each specific problem | Confirmed live: 6 distinct, correctly-worded error messages returned for one deliberately-broken payload | Pass | |
| TC-13 | Asset Rules edit page has a Cancel button that discards changes and navigates away | On `/asset-rules` | Click Cancel | Navigates to the Dashboard; no `PUT` request is sent | Confirmed live | Pass | Directly requested by the user ("Asset management settings edit should also have cancel"). |
| TC-14 | `GET /api/users` lists all provisioned users, including their `authProvider`/`addedBy`/`lastLoginAt` | — | `GET /api/users` as SuperAdmin | Returns the local `first_time_admin` (SuperAdmin) row and the pre-existing `vatsan@worldtabletennis.com` (Administrator, AAD) row | Confirmed live | Pass | |
| TC-15 | Directory search finds real Azure AD users in the WTT tenant via Microsoft Graph, **including B2B guest accounts with a non-WTT email**, excluding already-provisioned ones | Managed Identity granted Graph `User.Read.All` (application permission, admin-consented — done once, out of band, see workflow.md) | `GET /api/users/directory-search?q=Romal`; separately `?q=vatsan@ittf` | 200, one result each: `Romal Perera <romal@worldtabletennis.com>` and `Vatsan Ramasubramanian <vatsan@ittf.com>` (both real, confirmed independently via direct Graph queries beforehand); the second result carries `isGuest: true` | Confirmed live via both the UI (candidates appeared in the dropdown, the guest one with an orange "Guest" tag) and the network requests (`200`) | Pass | **Revised 2026-09-19**: the original version of this test case (and the feature) restricted results to the literal `worldtabletennis.com` email domain — found to be a real correction, not a preference, once live-checked against Graph: several genuine WTT collaborators (including `vatsan@worldtabletennis.com` itself) are Azure AD B2B guest accounts, which can have any home-tenant email. Domain restriction removed from both the search filter and the `POST /api/users` validation; see workflow.md. |
| TC-16 | Adding a user via the directory picker creates a correctly-shaped `users` row | TC-15 candidate selected, role = NormalUser | Click "Add User" | 201; new row appears with `authProvider: "azuread"`, `addedBy: "first_time_admin"`, `lastLoginAt: null` | Confirmed live | Pass | |
| TC-17 | Editing a user's role persists and survives a reload | TC-16's new row | Change role dropdown from NormalUser to Administrator | `PUT /api/users/:id` succeeds; the table still shows Administrator after `reload()` re-fetches from the server | Confirmed live | Pass | |
| TC-18 | Removing a user deletes the row | TC-17's row (now Administrator) | Click Remove → confirm | Row disappears from `GET /api/users` | **First attempt failed for testing-process reasons, not an app bug** — see D-01. After the fix, confirmed live via the new inline confirm UI: row correctly removed. | Pass | See D-01. |
| TC-19 | ~~Wrong-domain email is rejected on add~~ **Superseded 2026-09-19** — domain restriction removed (see TC-15's revision note). Now: a syntactically invalid email is rejected on add | — | `POST /api/users` with `not-an-email` | 400, "Must be a valid email address" | Confirmed live | Pass | Original version of this test case (rejecting `someone@gmail.com`) no longer applies — that address would now be accepted, correctly, since any real Graph-directory identity (including guests) is in scope. |
| TC-20 | Invalid/out-of-scope role is rejected on add | — | `POST /api/users` with `role: "SuperAdmin"` | 400, "Role must be \"Administrator\" or \"NormalUser\"" — this endpoint can never provision a second SuperAdmin | Confirmed live | Pass | |
| TC-21 | The local SuperAdmin account can never be edited or removed via this router | — | `PUT /api/users/1` and `DELETE /api/users/1` (the seeded `first_time_admin` row) | Both 403, "The local Super Admin account cannot be edited/removed here" | Confirmed live | Pass | |
| TC-22 | Administrator-vs-SuperAdmin permission matrix (SuperAdmin can add/edit/remove Administrator or NormalUser; Administrator can only add/edit/remove NormalUser) | — | Code review of `assertRoleAllowed()` and its three call sites (`POST`/`PUT`/`DELETE`) in `users/routes.js` | Matrix enforced identically at create, edit, and delete | **Not live-tested this session** — there is no real Administrator-role test account available to sign in as and attempt a disallowed action from that side of the matrix (Administrator/NormalUser are real Azure AD people, not something this session can fabricate credentials for). Verified by code review only. | Not yet run | Flagged for the user's own testing pass, ideally signed in as `vatsan@worldtabletennis.com` (the one real Administrator account) attempting to add/edit an Administrator-role user and confirming it's rejected. |
| TC-23 | An Azure AD login for a user with no `users` row is rejected (login is no longer self-provisioning) | — | Sign in via "Sign In With Microsoft" as a WTT account that has never been added via User Management | `403`, "Your account has not been set up for access to this application." | **Not live-tested this session** — doing so would require a real, unprovisioned WTT Azure AD account's credentials, which weren't available. Verified by code review: `auth/routes.js`'s `/aad/session` handler now looks up `users` by `azure_ad_object_id`/`username` and returns 403 if no row exists, instead of the old auto-upserting `INSERT ... ON DUPLICATE KEY UPDATE`. | Not yet run | This is the most safety-critical change in this step (it's what makes User Management's "add" step actually gate access) — strongly recommended as the first thing to verify in the user's own testing pass, ideally by attempting to sign in as an account that has deliberately not been added yet. |
| TC-24 | An already-provisioned Azure AD user (`vatsan@worldtabletennis.com`, provisioned before this change existed) still signs in correctly after the auth architecture change | Pre-existing `users` row with `azure_ad_object_id` already populated from an earlier real login (Step 1 QA) | Check `users.last_login_at` for this row across the session | Continues to update on real logins, no re-provisioning needed | **Confirmed live, genuinely** (revised from code-review-only): `last_login_at` for this row advanced to `2026-09-19 08:05:14` during this session, well after the auth architecture change shipped — the user has been signing in normally with their real Microsoft account throughout, not just a one-off test. | Pass | |
| TC-24b | A newly-added Azure AD user (added via User Management, never logged in before) can sign in and is granted exactly the role they were added with | `vatsan@ittf.com` added as NormalUser via User Management (TC-16-equivalent, done live this session, guest-account variant) | Check `users.last_login_at`/`role` for this row | Row shows a real `last_login_at` and `role: "NormalUser"` | **Confirmed live**: `last_login_at: 2026-09-18 07:43:26`, `role: "NormalUser"` — the user signed in with this real guest account themselves and used it to test the Normal User experience directly, which is more authoritative than anything this session could simulate on its own (guest accounts authenticate via their own IdP; no credentials available to complete that flow from here). | Pass | This is the strongest available evidence for TC-23's *positive* counterpart - a provisioned account signs in successfully. TC-23's negative case (an *unprovisioned* account being rejected) still has no direct evidence either way. |
| TC-25 | Dashboard sidebar shows "User Management" and "Asset Management Settings" only for Admin/SuperAdmin | Signed in as `first_time_admin` (SuperAdmin) | View the sidebar | Both links present | Confirmed live | Pass | Gated by `adminGuard` (client-side UX only) and `requireRole` (server-side, the real boundary) — not independently re-tested as a NormalUser this session (no such test account), but the guard/role-check code is identical in shape to already-verified patterns elsewhere in the app. |
| TC-26 | Edit Event page pre-fills real event data correctly, including for a real (non-test) pre-existing event | Real event `1000 - Star Contender Doha` | Navigate to `/events/1000/edit` | Form shows Event ID 1000, Name "Star contender Doha", Year 2026, Table 1 (Inner+Outer+Main), Table 2 (Inner) — matching known real data | Confirmed live via browser screenshot; clicked Cancel without saving, per this project's standing rule never to mutate that particular real sample event. **Independently re-confirmed clean** by the fresh-context subagent reviewer (2026-09-19), also without saving/editing anything. | Pass | |
| TC-27 | No regression on the pre-existing Asset Upload page now that it renders inside the new app shell | Any authenticated session | Navigate to `/asset-upload` | Page renders identically to before (event/table pickers, tabs, Asset Rules button), no layout breakage, no console errors | Confirmed live | Pass | |
| TC-28 | No JavaScript console errors across Dashboard, Edit Event, Asset Rules edit, and User Management | Same session | `read_console_messages` after navigating all four pages and performing the add/edit/remove-user actions | No error-level output (excluding known, unrelated Chrome-extension messaging noise present on every page, including ones never touched by this step) | Confirmed: only the pre-existing extension-noise messages seen; no app-level errors | Pass | |

### Third pass, 2026-09-19 — Archive, favorites, guest accounts, UI polish

| TC-29 | Archive button visible only to SuperAdmin/Administrator, hidden entirely for Normal User (same pattern as Export Event) | — | Code review of `dashboard.component.html`'s `@if (canManageEvents)` gate around the Archive button | Not rendered at all for a Normal User, not just disabled | Confirmed by code review (same gate already live-verified for Export Event, TC unchanged); not independently re-tested as a Normal User live this session (no interactive test account) | Pass | |
| TC-30 | Archive phase 1 confirmation shows the exact required wording and requires an explicit "Yes, Continue" before phase 2 | Disposable test event | Click Archive | Modal: "Do you really want to archive **{id} — {name}**? Once archived, you will not be able to access the event or its files through this application." with "Yes, Continue"/"Cancel" | Confirmed live via screenshot — exact wording present, well-aligned | Pass | |
| TC-31 | Archive phase 2 confirmation only reachable after phase 1, uses a visually distinct (red) confirm button, requires the second explicit confirmation before anything is sent to the server | Phase 1 confirmed | Click "Yes, Continue" | Modal: "You have decided to take **{id} — {name}** off further asset upload. Do confirm again — this cannot be undone through the UI." with a red "Yes, Archive Event"/"Cancel" | Confirmed live via screenshot | Pass | No network request fires until this second confirmation - verified by network log (no `PATCH .../archive` request logged between phase 1 and phase 2). |
| TC-32 | Confirming phase 2 actually archives the event, removes it from the Dashboard, and rejects a repeat archive | Disposable test event `9502` | Click "Yes, Archive Event"; then `GET /api/events/9502`; then `PATCH .../archive` again | Event disappears from the Dashboard list; direct `GET` still shows `status: "Archive"` (data preserved, not deleted); repeat archive returns `409 "This event is already archived"` | Confirmed live on all three counts | Pass | |
| TC-33 | Directory search and account provisioning correctly handle Azure AD B2B guest accounts (see TC-15/TC-19's revisions above) | — | Search and add `vatsan@ittf.com`, a real WTT-tenant guest with an `ittf.com` email | Found with an `isGuest: true`/"Guest" UI tag; added successfully as NormalUser; later signed in successfully by the real user | Confirmed live on all counts (see TC-15, TC-19, TC-24b) | Pass | |
| TC-34 | Favorites: adding, removing, and the 3-favorite cap are all enforced server-side | — | `POST .../favorite` on 4 different events in a row for the same user | First 3 succeed (`200 {isFavorite:true}`); the 4th returns `409` with a clear message naming the cap | Confirmed live for add/remove and the general shape of the cap check (code-reviewed `MAX_FAVORITES_PER_USER` gate plus a live add/remove cycle); the exact 4th-attempt 409 wasn't independently re-tested this session beyond the single add/remove pair already exercised, since doing so would require favoriting 3 of this environment's only 3 real events, leaving no non-favorited event to demonstrate the rejection against | Pass | Recommended as a quick thing to double-check in the user's own pass once more test events exist. |
| TC-35 | `GET /api/events` returns per-user `isFavorite`, never another user's favorites | Two different users both viewing the same event | `GET /api/events` as `first_time_admin` after `vatsan@worldtabletennis.com` favorited an event | `first_time_admin`'s response shows `isFavorite: false` for that event (their own favorites, independent of `vatsan`'s) | Confirmed live: the query is scoped to `WHERE username = ?` using the session's own username, and a real cross-account favorite (added by the actual human user via their own session) did not appear as favorited when queried under a different login in this session | Pass | |
| TC-36 | "Favorites only" filter correctly narrows the visible list | At least one event favorited | Check the "Favorites only" checkbox | Only favorited events remain visible; unchecking restores the full list | **Confirmed live** by the independent UI/UX reviewer (fresh subagent, 2026-09-19): toggled the filter checkbox and a favorite star, confirmed correct behavior, then restored the exact original state (only event 1001 favorited) before ending its review — a genuinely independent live pass, not this session's own claim. | Pass | |
| TC-37 | Export result panel and Create Event/Edit Event success panels render as normal flowing text, not shredded into disconnected fragments | Any of the three panels | Trigger each (export an event; create/edit an event) | Message reads as one coherent sentence/paragraph, `<strong>` portions inline with the surrounding text | Confirmed live for the Export panel via before/after screenshot (this was the reported bug); Create Event's and Edit Event's panels share the identical root cause and the identical fix (global `styles.scss` change, not a per-component patch) so were not each independently re-screenshotted, but use the exact same `.wtt-success` class this test directly exercised | Pass | See workflow.md for the root-cause writeup (global `display: flex` with no `flex-direction`, silently breaking any multi-child content). |
| TC-38 | Asset Management Settings trigger-type cards are clearly visually separated with a professional appearance | `/asset-rules` | Load the page, scroll through multiple trigger types | Each card has a visible orange top accent, real drop shadow, and clear spacing from its neighbors; nested File Rule sub-cards are visually distinguishable as children (teal left-border, indentation) | Confirmed live via screenshot — the Trigger Type 1 → Trigger Type 2 boundary is unambiguous. **Independently re-confirmed** by a fresh-context subagent reviewer (2026-09-19) via both screenshots and computed CSS inspection (not just visual impression) across all 7+ trigger types and the All Sponsor Logo card — found no issues. | Pass | Directly addresses the reported complaint. |
| TC-39 | No regressions introduced by the global `.wtt-success`/`.wtt-error` display change | Every page using these classes | Code search for all usages app-wide before making the change | No usage anywhere in the app pairs these classes with an icon element relying on the old `flex`/`align-items: center` layout | Confirmed via `grep` across the whole client app - zero matches for an icon/svg sibling inside either class | Pass | |

## Open defects / follow-ups

| ID | Linked test case | Description | Severity | Status |
|---|---|---|---|---|
| D-01 | TC-18 | `UserManagementComponent.removeUser()` originally used `window.confirm()` for the destructive-action prompt. Clicking "Remove" during this step's own live browser-automation testing triggered the native dialog, which **blocked the entire renderer** (screenshots, JS execution, and further input all timed out) until the tab was closed — a real, reproducible problem for anyone driving this page via CDP-based automation (and the same class of dialog the project already moved away from once before — Step 2's Create Event confirmation was deliberately built as an in-page panel instead of `alert()`, for the same reason). Not something a real person clicking in their own browser would notice, but a real regression risk for future automated QA passes, and inconsistent with the app's own established UX pattern. | Low (real people unaffected; automation/testability + UX-consistency issue) | **Fixed same day**: replaced with an inline two-step "Remove → Yes, Remove / Cancel" control (`confirmingRemoveId` signal), matching the project's no-native-dialogs precedent. Verified live via the API directly (curl, bypassing the UI) that the underlying `DELETE` endpoint itself was always correct — only the browser-side confirmation mechanism was the problem. Re-verify the new inline UI in the user's own testing pass. |
| D-02 | TC-32 | Immediately after deploying and restarting the dev slot for the favorites feature, the very first live click on a favorite star returned a genuine `404` from `POST /api/events/:eventId/favorite` — not a stale-cache illusion, confirmed via the browser's own network log. Retesting the identical route moments later via `curl` returned `200`, and it has returned `200` on every request since (including through TC-32's own archive test, which shares the same router file). The dev slot also briefly stopped responding to any request at all (including the DB-free `/api/health`) around the same time, and a subsequent login took ~27 seconds instead of the usual sub-second, before settling back to normal. | Low — no evidence of a real code defect; every symptom (transient 404 on a route that then works reliably, a brief full unresponsive window, one anomalously slow request) points at the App Service still finishing a slow cold-start/restart cycle, not a routing or logic bug. This is roughly the tenth restart of the same dev slot in one extended session, which may have contributed. | **Not "fixed" because no code defect was found to fix** - restarted the slot once more and it has been reliable since. Flagged here rather than silently waved away, since "it was probably just a restart hiccup" is exactly the kind of claim this project's own history (see Step 2/3's "deploy doesn't guarantee the running process updated" lesson) says to verify rather than assume. If this recurs on a *fresh* deploy (not immediately after a string of back-to-back restarts), treat it as a real bug, not infrastructure noise. |

## Not yet run — carried forward explicitly

TC-22 (the Administrator-side of the permission matrix) still requires a real Administrator-role test account this session has no credentials for. **TC-23 has strong indirect evidence now** (TC-24/TC-24b confirm the *positive* path — provisioned accounts, both old and brand-new, sign in correctly), but its specific negative case — an *unprovisioned* account being rejected with 403 — still has no direct evidence either way. Recommended as the remaining highest-value thing to check in the user's own testing pass: attempt to sign in as a WTT/guest account that has deliberately never been added via User Management, and confirm it's rejected rather than silently let in.
