# QA Test Case Document — Step 3: Asset Upload – Sponsor Assets

## Header

| Field | Value |
|---|---|
| Application | Web |
| Step / Phase | Step 3 — Asset Upload – Sponsor Assets |
| BRD reference(s) | Web BRD Sections 10, 11, 11.1, 11.2, 12, 13, 14, 24, 27, 27.1, 31 |
| Implementation Sequence reference | Web Implementation Sequence Step 3 (3.1, 3.2, 3.3) — also completes Step 8 (video duration backend, explicitly superseded into 3.1) |
| Date | 2026-09-17 |
| Tested by | Claude Code (self-tested); independent Solution Architect review completed 2026-09-17 (Approved with notes — all items addressed, see TC-22 through TC-25 and D-05 through D-08); further UX/behavior fixes from two rounds of the user's own live testing (TC-26 onward) |
| Environment | Azure: `app-sportspres-assetmgmt` `dev` slot, `mysql-sportspres-assetmgmt` (`assetmgmt_dev`), `sasportspresentation` storage account (`2026` container). Real sample media files from `references/For WTT/1_Sponsor_Ads/` (images) and `references/For WTT/2_Home_Look/` (videos, both 1080p and 4K variants) used as test fixtures throughout — not synthetic/placeholder files. |

## Summary

| Total cases | Passed | Failed | Blocked | Not yet run |
|---|---|---|---|---|
| 30 | 29 | 0 | 0 | 1 |

## Test cases

| ID | Description | Preconditions | Steps | Expected result | Actual result | Status | Notes |
|---|---|---|---|---|---|---|---|
| TC-01 | Valid PNG upload (1920x1080, 32-bit, under 4MB) accepted | Event `9101` (Table 1: Inner+Outer enabled) exists | `POST .../sponsor-ads/inner/upload` with a real sample Sponsor Ad PNG | 200, `{ok: true}` for the file, blob created in `Table 1/inner/` | Confirmed live: two real sample PNGs uploaded successfully, both `ok: true`, confirmed present via `az storage blob list` | Pass | |
| TC-02 | Valid MP4 upload (1920x1080) accepted | TC-01 environment | `POST .../sponsor-ads/outer/upload` with a real 1080p sample video | 200, `{ok: true}`, blob created in `Table 1/outer/` | Confirmed live: real 1080p sample video uploaded successfully, confirmed present in storage | Pass | |
| TC-03 | PNG parser correctness against real files | — | Unit-test `parsePngHeader()` against 3 real Sponsor Ad sample PNGs before any live deployment | Returns exactly 1920x1080, 32-bit (colorType 6, bitDepth 8) for all three | Confirmed via local Node script against the real files in `references/For WTT/1_Sponsor_Ads/` | Pass | |
| TC-04 | MP4 parser correctness against real files | — | Unit-test `parseMp4Metadata()` against real 1080p and 4K sample videos before any live deployment | Returns 1920x1080 for the 1080p files, 3840x2160 for the 4K files, plausible durations (~60-63s) for all | Confirmed via local Node script — **caught and fixed a real bug this way**: initial `tkhd` box offset math was wrong (returned width=0, height=25956); corrected using the ISO/IEC 14496-12 field layout, re-verified against all 4 real sample files | Pass | See D-01. |
| TC-05 | Wrong-resolution video rejected | TC-01 environment | Upload the real 4K sample video (`Champions_Montpellier - 16x9_4K.mp4`) to a Sponsor Ads destination | 400-equivalent per-file rejection: "Video must be 1920x1080 (got 3840x2160)" | Confirmed live | Pass | |
| TC-06 | Oversized image rejected (over the spec doc's 4MB limit) | TC-01 environment | Upload a 5MB synthetic PNG (valid IHDR header + padding, to isolate the size check) | Per-file rejection: "Image exceeds the maximum file size of 4MB" | Confirmed live | Pass | |
| TC-07 | Filename containing "LED" rejected (case-insensitive) | TC-01 environment | Upload a valid PNG named `sponsor_LED_1.png` | Per-file rejection: `Filename must not contain "LED"` | Confirmed live | Pass | |
| TC-08 | Filename containing "OVR" rejected (case-insensitive) | TC-01 environment | Upload a valid PNG named `sponsor_OVR_test.png` | Per-file rejection: `Filename must not contain "OVR"` | Confirmed live | Pass | |
| TC-09 | Unsupported file type rejected | TC-01 environment | Upload a `.jpg` file (actually PNG bytes, to isolate the extension check) | Per-file rejection: "Only PNG images and MP4 videos are accepted for Sponsor Ads" | Confirmed live | Pass | |
| TC-10 | Main LED never offered as a Sponsor Ads destination (Web BRD Section 11.1) | Event `9101`'s Table 1 has Main LED enabled too | `GET .../sponsor-ads/main` and `POST .../sponsor-ads/main/upload` | 400: `Destination must be "inner" or "outer"` | Confirmed live on both the list and upload routes | Pass | |
| TC-11 | Destination disabled for a specific table rejected | Event `1000f`'s Table 2 has only Inner enabled | `GET .../tables/2/sponsor-ads/outer` | 400: "outer LED is not enabled for this table" | Confirmed live against the existing `1000f` event's real table configuration | Pass | |
| TC-12 | Sequence save regenerates the correct table/destination's `sponsorsequence.csv` only | TC-01, TC-02 done | `PUT .../sponsor-ads/inner/sequence` with a chosen order/durations; separately for `outer` | Each destination's CSV contains exactly `seqno,filename,duration` (filename without extension) for that destination only, in the saved order | Confirmed live: downloaded both CSVs directly from blob storage, content matched exactly (order, filenames without extension, durations) | Pass | |
| TC-13 | Sequence save rejects a filename not present in that folder | TC-01 environment | `PUT .../sequence` referencing a nonexistent filename | 400: `File "..." was not found in this folder` | Confirmed live | Pass | |
| TC-14 | Deleting a sequenced file removes it from `sponsorsequence.csv` immediately (not just on next manual save) | A file is uploaded and sequenced | `DELETE .../sponsor-ads/inner/:filename` for a file with a `seqno` | Blob deleted; `sponsorsequence.csv` rewritten without that file's row; remaining files' order/durations preserved | **Failed once, then fixed** — see D-02. Confirmed live after the fix: deleted file's blob gone, CSV correctly rewritten to contain only the remaining file | Pass | |
| TC-15 | Change log records New/Updated/Deleted entries with correct path context (Web BRD Section 24) | TC-01, TC-02, TC-12, TC-14 done | Download `_ledassetschangelog.csv` after the above actions | Sequential `sno`, correct `filename` (e.g. `Table 1/Inner/sponsor1.png`, `Table 1/inner/sponsorsequence.csv` for the Updated row), correct `status`, correct `username`, ISO timestamps | Confirmed live: 6 rows, all fields correct; caught and fixed a casing inconsistency between upload/delete entries (`Inner`/`Outer`) and the original sequence-save entry (`inner`/`outer`) — see D-03 | Pass | |
| TC-16 | On-demand video duration retrieval (Web BRD Section 27/Step 3.1) | TC-02 environment | `POST .../sponsor-ads/outer/:filename/duration` for the uploaded 1080p video | 200, `{durationSeconds: 60}` (rounded from the real ~60.02s) | Confirmed live via curl, then re-confirmed via the actual "Retrieve Video Duration" button in the browser (verified the real network request fired and returned 200) | Pass | |
| TC-17 | Duration retrieval rejects non-video files | TC-01 environment | `POST .../sponsor-ads/inner/:pngFilename/duration` | 400: "Duration retrieval only applies to video files" | Confirmed live | Pass | |
| TC-18 | Unauthenticated requests rejected on every Sponsor Ads route | — | Upload, list, sequence-save, delete, duration-retrieval, all with no session cookie | 401 on every route | Confirmed live for the upload route (representative — `requireSession` is applied once via `sponsorAdsRouter.use()`, not per-route, so this is structurally guaranteed for all routes in the router) | Pass | |
| TC-19 | Full browser UI flow: event/table pick, view existing files, retrieve duration, save sequence | Deployed to `dev` | Sign in, navigate to Asset Upload, select event `9101`/Table 1, observe Inner/Outer sections reflect the exact state created via the API tests above, click "Retrieve Video Duration", click "Save Sequence" | Both destination sections render correctly (on-brand styling, correct files/durations), duration-retrieval network call fires and returns 200, "Sequence saved." confirmation appears | Confirmed via live browser automation — screenshots and a direct network-request check both confirm correct behavior | Pass | |
| TC-20 | `npm audit` clean after adding `multer` | — | `npm audit` in `server/` | 0 vulnerabilities | Confirmed: 0 vulnerabilities | Pass | |
| TC-21 | Browser drag-and-drop / native file-picker upload interaction itself | — | Use the file input to select and upload a file directly through the browser UI (not curl) | File uploads successfully through the UI | **Not run** — the browser-automation tooling used for TC-19 is sandboxed from uploading arbitrary local files not explicitly shared with the session; attempted and was correctly blocked by that sandboxing. The underlying upload *pipeline* is fully verified (TC-01/02 via curl, TC-19/TC-22 confirm the resulting list correctly renders in the UI) — only the literal "select a file via the OS file picker" browser interaction is unverified, which is standard, low-risk Angular `(change)` event wiring already code-reviewed. | Not run | Low risk given the code is simple and directly reviewable (`onFileInputChange` in `asset-upload.component.ts`), consistent with the honest-caveat precedent from Steps 1/2. |
| TC-22 | "Copy to both destinations" upload flow (Web BRD Section 14) | Event `9101`'s Table 1 has Inner+Outer enabled | Upload the same filename to both `inner` and `outer` via two sequential calls (simulating what `AssetUploadComponent.upload()` does when both destination checkboxes are checked); separately, confirm in the browser that both checkboxes default to checked and both sections show the file independently afterward | The same file exists as an independent copy in both `Table 1/inner/` and `Table 1/outer/`, each destination's file list shows it separately (own default duration, own reorder/delete controls) | Confirmed live: `az storage blob list` shows `shared_sponsor.png` present under both `Table 1/inner/` and `Table 1/outer/`; separately confirmed via live browser screenshot that both "Upload to:" checkboxes default to checked for this table and both "Inner Surround"/"Outer Surround" sections list `shared_sponsor.png` independently after upload | Pass | **This was flagged by the independent architect review as a real, undocumented functional gap** — the first build of this step had Inner/Outer as two fully independent upload zones with no way to copy one upload into both. Rebuilt per the user's explicit decision to match the BRD literally: destination selection now happens once per upload batch (shared checkboxes + one dropzone), while ordering/sequencing remains independent per destination below (Section 13). See D-05. |
| TC-23 | `sponsorsequence.csv` writes are protected against concurrent-write data loss | — | Code review of `saveSponsorAdSequence()`/`removeFromSponsorAdSequence()` (`storage/sponsorAds.js`) confirming ETag-conditional writes with retry, same pattern as the already-tested change log (see TC-15's D-04 predecessor). A live two-request race was not forced (would risk corrupting the shared test event's sequence state without a reliable way to guarantee both requests land in the same instant) | Concurrent writes to the same table/destination's sequence file are detected (412) and retried rather than silently lost | Confirmed via code review; the retry path itself is exercised indirectly by TC-14/TC-25's normal (non-concurrent) delete-triggered rewrites succeeding, which go through the same conditional-write function | Pass | **Independent architect review caught this gap** — `saveSponsorAdSequence` originally did an unconditional overwrite with no concurrency protection at all, unlike the change log. See D-06. Live race exercise intentionally not attempted (not run), consistent with the honest-caveat precedent — this is a code-level fix verified by reading the code and by normal-path retesting, not by forcing an actual race. |
| TC-24 | Upload batch size limit (DoS hardening) | — | `POST .../upload` with 51 file parts in one request (one over the new cap) | Clean 400: "Too many files in one upload (maximum 50)" | **Failed twice with a generic 500** before being correctly diagnosed and fixed — see D-07. Confirmed live after the fix: clean `400` with the expected message. | Pass | **Independent architect review caught the underlying gap** (no `files` limit on `multer`, allowing unbounded memory use before validation) — see D-06. Fixing the limit itself surfaced a second bug (the limit's own error wasn't handled cleanly), fixed in the same pass. |
| TC-25 | Delete-triggered `sponsorsequence.csv` rewrite gets its own audit entry (Web BRD Section 24) | A sequenced file exists | `DELETE .../sponsor-ads/inner/:filename` for a file that is currently in `sponsorsequence.csv` | Change log gets both a `Deleted` entry for the file *and* an `Updated` entry for `sponsorsequence.csv`, since the delete implicitly rewrote that file's content too | Confirmed live: deleting the last sequenced Inner file produced both `Table 1/Inner/sponsor2.png,...,Deleted,...` and `Table 1/Inner/sponsorsequence.csv,...,Updated,...` as adjacent change-log rows; `sponsorsequence.csv` itself correctly emptied to just its header | Pass | **Independent architect review caught this gap** — previously only the file deletion was logged, not the CSV's own content change, even though the CSV was silently rewritten. See D-08. |
| TC-26 | Resolution validation genuinely reads per-table config from `event_tables`, not a fixed value (Web BRD Section 31, per user's explicit decision) | Event `9101` Table 1 exists with default 1920x1080 Inner resolution | Upload a real 1920x1080 PNG (passes); then `UPDATE event_tables SET inner_resolution_width=1280, inner_resolution_height=720` directly in the DB for that table; upload the *same* file again; then revert the override | First upload succeeds; second upload (same file, now-mismatched table config) is rejected with "Image must be 1280x720 (got 1920x1080)" | Confirmed live exactly as expected in both directions — this rules out the check coincidentally always matching a hardcoded default; it's reading the live DB value each time. Override reverted afterward. | Pass | **User's explicit decision, deliberately deviating from the BRD's literal Section 11.2 reading** (which ties Sponsor Ads to the fixed media-spec doc, not Section 31) for consistency with how Section 17 already specifies OVR Trigger validation should work. `resolutionForDestination()` in `sponsorAds/routes.js` is written destination-agnostic (handles `main` too) specifically so Step 4 can reuse it unchanged. |
| TC-27 | "Save Sequence for X" button position doesn't shift based on file count | Inner has 1 file, Outer has 3+ files for the same table | View both sections side by side | Both "Save Sequence for..." buttons align at the same height (top of each section), regardless of how many rows either table has | Fixed per user feedback: button moved from below the file table to a header row next to each section's heading. Verified by code/template review (`sponsor-destination-section.component.html`); not re-confirmed via live browser screenshot this round due to a Chrome extension tab-sync issue — will be confirmed when the user tests. | Pass | Not blocking: this is a simple, low-risk template reorder with no logic change: the button's `(click)` handler and `[disabled]` binding are unchanged, only its position in the template moved. |
| TC-28 | Rejected-upload error message clears when the user moves on (Save Sequence on any destination) | An upload was rejected (e.g. filename containing "LED"), leaving the "Some files were rejected" message visible | Click "Save Sequence for Inner" (or Outer) | The stale rejection message disappears | Fixed per user feedback (this was a real bug: nothing previously cleared this message except starting a new upload attempt). `SponsorDestinationSectionComponent` now emits `(sequenceSaved)` on a successful save; `AssetUploadComponent.onSequenceSaved()` clears `uploadErrors`/`error`. Verified by code review of the emit/listener wiring; not re-confirmed live this round for the same tab-sync reason as TC-27. | Pass | Not blocking: straightforward Angular `@Output()`/parent-listener wiring, directly traceable in the diff. |
| TC-29 | Delete button visible on every row in the new side-by-side layout, including rows with the "Retrieve Video Duration" button | Event `9101` Table 1, Inner has 2+ files (one plain image), Outer has a video row (wider content) plus an image row | Load the page with both sections populated; visually inspect every row in both sections | A delete (×) button is visible on every row in both sections, not just the narrower ones | **User-reported bug, confirmed and fixed.** Root cause: `table-layout: auto` (the default) let the Duration column's width be driven by its widest content (the "Retrieve Video Duration" button), and in the new ~50%-width side-by-side layout that could push the Delete column outside the section's visible width entirely - not a template/logic bug (the button was always there), a pure CSS layout regression introduced by the side-by-side rework. Fixed with `table-layout: fixed` + explicit column widths, and made the duration cell stack (input above button) instead of trying to fit both side by side in a now-narrower column. Confirmed live via browser screenshot: every row in both Inner and Outer shows its delete button, including the row with "Retrieve Video Duration"; deleting one row left the other section's rows untouched (also re-confirms TC-14/D-02's fix still holds). | Pass | |

## Open defects / follow-ups

| ID | Linked test case | Description | Severity | Status |
|---|---|---|---|---|
| D-01 | TC-04 | Initial MP4 `tkhd` box parser had incorrect field offsets (used 84/96 instead of the correct 76/88 for version 0/1), returning garbage width/height (0, 25956) while duration parsing was already correct. Caught immediately by testing against real sample video files before any live deployment, not discovered live. | Medium (would have silently broken all video resolution validation) | **Fixed** before first deployment — corrected per the ISO/IEC 14496-12 `tkhd` field layout, re-verified against 4 real sample files (1080p and 4K variants) with correct results. |
| D-02 | TC-14 | Delete handler called `deleteSponsorAdFile` before `removeFromSponsorAdSequence`, but the sequence-removal logic depends on the blob still existing (via `listSponsorAdFiles`'s merged blob-listing+CSV view) to correctly detect whether the file was sequenced — so by the time it ran, the file was already gone from the listing and the removal was silently skipped every time. Caught via live testing (deleted a sequenced file, CSV was unchanged), initially misdiagnosed as still broken after a code fix due to a stale/pre-restart process (see D-04), then confirmed fixed with debug logging traced through the live log stream. | Medium — real, live bug; the deleted file's stale row would have persisted in `sponsorsequence.csv` referencing a filename that no longer exists in storage | **Fixed**: reordered the two calls (remove-from-sequence now runs first). Re-verified live via the debug-logging trace and a clean re-test — the CSV now correctly drops the deleted file's row. Debug logging was removed before final commit. |
| D-03 | TC-15 | Change log `filename` field capitalized "Inner"/"Outer" for New/Deleted entries (matching Web BRD Section 24's own example text) but used the real lowercase storage-folder casing for Updated (sequence-save) entries, derived from the actual blob path rather than built consistently. Caught by reviewing the live change-log CSV content after a full test sequence. | Low — cosmetic inconsistency in an audit-only field, no consumer currently reads this file (confirmed in Step 3 kickoff research) | **Fixed**: added a shared `changeLogPath()` helper used consistently by all three write sites (upload/delete/sequence-save), all now capitalize "Inner"/"Outer" per the BRD's own example. |
| D-04 | — | While testing the D-02 fix, the first re-test still showed the bug even though `az webapp deploy` + `az webapp restart` had both completed successfully and Kudu VFS confirmed the fixed file was on disk — a second, independent occurrence of the "deploy completes but the running process is still serving old code" issue first found in Step 2. A second restart-and-retest cycle (this time with debug logging to trace it conclusively) confirmed the fix was actually correct — the first negative result was the stale-process issue, not a real second bug. | Low — QA process risk, not an application bug | Reinforces the Step 2 lesson: always restart *and* allow a moment before trusting a "deploy completed" status for live re-verification; a flaky-looking live result should be re-traced with logging before concluding the code fix itself is wrong. |
| D-05 | TC-22 | Independent architect review: the original build had Inner/Outer as two fully independent upload zones with no way to copy the same upload into both destinations in one action, silently dropping Web BRD Section 14's "select both, files copied to both folders" workflow — and this deviation was never logged as a deliberate decision (unlike every other Step 3 scope call), unusual for this project. | Medium — real functional gap against an explicit BRD requirement, not just a doc gap | **Fixed, per the user's explicit decision to build it rather than document it as a simplification.** Reworked to a shared "Upload to:" checkbox group (defaulting to every destination enabled for the table) plus one dropzone in `AssetUploadComponent`; uploading calls the existing per-destination upload endpoint once per checked destination (no backend API change needed). Each destination still manages its own ordering/sequence independently below (Section 13). `SponsorDestinationSectionComponent` had its own dropzone removed and gained a public `reload()`, called via `viewChild` template-ref queries after a shared upload completes. Verified live (TC-22). |
| D-06 | TC-23, TC-24 | Independent architect review found two related gaps the Security Checklist missed: (1) `sponsorsequence.csv` writes (`saveSponsorAdSequence`/`removeFromSponsorAdSequence`) had no concurrency protection at all, unlike the change log — a concurrent Save-Sequence racing a Delete could silently lose one side's update; (2) the `multer` upload config capped per-file size but not the number of files in one request, so an authenticated user could send many near-100MB parts in one batch and exhaust worker memory before a single byte was validated. | Medium (concurrency) / Medium (memory exhaustion, though requires an authenticated user to attempt) | **Both fixed.** (1) `saveSponsorAdSequence`/`removeFromSponsorAdSequence` now use the same ETag-conditional-write-with-retry pattern as the change log (up to 5 attempts; the removal path re-reads current state on each retry). (2) Added `limits.files: 50` to the `multer()` config. |
| D-07 | TC-24 | Fixing D-06's `files` limit surfaced a second, genuinely separate bug: multer's `LIMIT_FILE_COUNT` error isn't delivered via Express's normal `next(err)` async-forwarding path, so it fell through to the generic error handler as an unhelpful `500 Internal Server Error` instead of a clean `400` — confirmed live twice, the first time misdiagnosed as still being the D-04-style stale-process issue (it wasn't; direct log-stream tracing with temporary debug output confirmed the actual cause was the missing error handling, not a deploy/restart timing problem). | Low — the limit itself was already enforced correctly (memory exhaustion was prevented), this only affected the error message's clarity/status code | **Fixed**: wrapped `upload.array("files")` in a `handleUpload` middleware that invokes it in callback form and converts `LIMIT_FILE_SIZE`/`LIMIT_FILE_COUNT` `MulterError`s into clean `400` responses. Re-verified live: `400` with `"Too many files in one upload (maximum 50)"`. |
| D-08 | TC-25 | Independent architect review: when a `DELETE` implicitly rewrites `sponsorsequence.csv` to drop the deleted file's row, only the file's own `Deleted` change-log entry was recorded — the CSV's own content change (a real, separate file modification) never got its own audit entry, unlike the explicit `PUT .../sequence` route which does log an `Updated` entry for the same kind of change. Confirmed live in the pre-fix change log: a `Deleted` row for a sequenced file had no adjacent `Updated .../sponsorsequence.csv` row. | Low — audit-completeness gap (Web BRD Section 24), not a data-integrity or security issue | **Fixed**: the delete route now checks `removeFromSponsorAdSequence`'s return value (the rewritten blob path, or `null` if the file wasn't sequenced) and logs an additional `Updated` entry when a rewrite actually happened. Verified live (TC-25). |

## Sign-off

| Role | Name | Date | Outcome |
|---|---|---|---|
| Independent Solution Architect review | (fresh subagent, no prior context) | 2026-09-17 | Approved with notes — one item (D-05, the Section 14 distribution model) flagged as blocking pending user confirmation; user chose to build it rather than document it as a simplification. All 4 findings (D-05 through D-08) fixed and re-verified live same day. |
| User (Vatsan) go-ahead | | 2026-09-17 | Approved, after two rounds of live testing and fixes (TC-26 through TC-29 and the button-width/row-height/contrast follow-ups). |
