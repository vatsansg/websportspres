# Web Application — Workflow / Progress Tracker

**This is a living document.** Claude Code must amend this same file after every step is completed (not create a new file) — updating status, completion date, what was actually built, and a link to that step's QA Test Case and Security Checklist documents. This is the single place the user checks to see overall progress.

Status values: `Not started` / `In progress` / `Awaiting user go-ahead` / `Complete`.

---

## Step 1 — Application, App Service and Database Environment Setup
- **Status:** Complete (user go-ahead given 2026-09-16)
- **Includes:** Azure kickoff questions answered, App Service, MySQL (Managed Identity auth), full DB schema, authentication/roles, Storage Account Managed Identity connectivity.
- **Completed on:** 2026-09-16
- **What was built:**
  - **Repo:** Standalone git repo at `Applications/web`, remote `github.com/vatsansg/websportspres`. Work done on `step-1-appservice-mysql-setup`, merged into `main` (13 commits) on user go-ahead. **Push to `origin/main` was blocked repeatedly by Claude Code's own safety layer** (alternating "Credential Leakage"/"Out-of-Place Publication" — a persistent gate, not a transient issue) — user pushed it themselves (`git push -u origin main`), confirmed successful 2026-09-16.
  - **Azure resources** (subscription `b239d404-c0c0-4098-a287-f9983f71d56b`, RG `rgsportspresentationsource`, region `southeastasia`):
    - App Service Plan `asp-sportspres-assetmgmt` (Linux, Standard S1); App Service `app-sportspres-assetmgmt` (production) + `dev` deployment slot, Node 24-lts runtime, each with its own system-assigned Managed Identity (prod `33a4e55a-eeb0-46a2-bd58-81ec7ed80c16`, dev `375425bf-bec2-4d1c-8b53-a5852174095c`), both granted **Storage Blob Data Contributor** scoped to `sasportspresentation` only
    - MySQL Flexible Server `mysql-sportspres-assetmgmt` (General Purpose, `Standard_D2ds_v4`, **MySQL 8.4.9** — upgraded in-place from 8.0.21 with user's confirmation), firewall-based network access with the standard "allow Azure services" rule plus the developer's own IP for admin scripts; two databases on the one server, `assetmgmt` (prod) and `assetmgmt_dev` (dev)
    - AAD Administrator on the MySQL server: `vatsan@worldtabletennis.com`; user-assigned managed identity `uami-mysql-sportspres-assetmgmt` attached to the server and granted **Directory Readers** by a WTT tenant admin, enabling full Managed-Identity MySQL auth for the App Service's own identities (via `bootstrap-mysql-aad.mjs`) — **confirmed live**
    - TLS 1.2 minimum + FTP disabled on both App Service slots; HTTPS-only on by default
    - Azure AD app registration `sportspresweb` (client ID `500d7462-f71f-4bed-98e1-27a03b32f999`, tenant `5bfafc67-078e-46b0-bc4f-4309032e4464`) with `Administrator`/`NormalUser` App Roles, wired via `AZURE_AD_TENANT_ID`/`AZURE_AD_CLIENT_ID` App Service settings
    - Remaining App Service settings set directly (not via chat): `MYSQL_HOST`, `MYSQL_AAD_USER`, `MYSQL_DATABASE`, `STORAGE_ACCOUNT_URL`, `STORAGE_TEMPLATES_CONTAINER`, `SESSION_JWT_SECRET` (generated per slot, never shown/committed), `MAX_VIDEO_UPLOAD_MB=100`, `CHANGE_LOG_EMAIL_RECIPIENT`
  - **Database schema** (applied to both `assetmgmt` and `assetmgmt_dev`): `users`, `events`, `event_tables`, `schema_migrations`. See "Deviations" below for why LED config lives on `event_tables`, not `events` (user-confirmed).
  - **Backend** (`server/`, Node.js/Express, ESM): Managed-Identity-only MySQL pool and Blob client (`DefaultAzureCredential`, no passwords/keys anywhere). Super Admin local login + change-password (bcrypt). Azure AD ID-token validation (`src/auth/azureAdAuth.js`) + session-exchange endpoint. Server-side RBAC middleware. Audit logging for failed auth attempts. Health-check endpoints. Runtime `/api/config` endpoint (no per-environment rebuild needed for AAD IDs). Explicit CSP allowing only `https://login.microsoftonline.com` in `connect-src`, everything else on helmet's secure defaults.
  - **Frontend** (`client/`, Angular 20 — deliberately not v21, see Security Checklist H1): Login page (local + "Sign In With Microsoft," MSAL public-client/PKCE, no client secret), Change Password page, an authenticated dashboard shell proving the auth/RBAC framework end-to-end (the real Dashboard/Event List is Step 6's job). Fully polished via `wtt-brand` + a bounded `ralph-loop` pass (see below) — WTT wordmark, orange/teal/blue accents, Roboto Bold headlines, button loading spinners, focus states, autofill-safe dark theme.
  - **Operator scripts** (`server/scripts/`, run by hand, not part of the running app): `create-databases.mjs`, `migrate.mjs`, `bootstrap-mysql-aad.mjs`, `test-led-constraints.mjs` (TC-10 regression check).
- **QA Test Case doc:** `docs/QA_Web_Step1_AppServiceMySQLSetup.md` (16 cases: 15 Pass, 1 Not Run — see doc)
- **Security Checklist:** `docs/Security_Web_Step1_AppServiceMySQLSetup.md`
- **Deviations from plan:**
  1. **LED configuration schema placement — confirmed by user.** BRD Section 5 lists LED booleans on `Events`; Sections 3.1/6/9/10/11.1 and decisively Section 25.4's export schema show LED config is per-**table**. Built `event_tables` (per-table LED booleans + resolution), not on `events` — user confirmed this is correct.
  2. **Implementation Sequence Step 1.4 vs. BRD Section 2.1.** Built on the BRD's authoritative version: only Super Admin has an app-level password; Administrator/Normal User are pure Azure AD.
  3. **App Service architecture.** One App Service Plan + `dev` deployment slot (not two separate Plans/Apps) — approved as part of the naming/architecture sign-off.
- **Real bugs found and fixed during this step** (chronological — each was found via live testing, not assumed):
  1. **`seedSuperAdmin()` startup crash.** A MySQL outage (expected while Directory Readers was pending) crashed the *entire* Node process, not just DB-dependent routes. Fixed: `index.js` now degrades gracefully.
  2. **Wrong AAD token type.** Frontend requested a Microsoft Graph access token; backend validated audience against our own client ID — would never match. Fixed: request the ID token instead.
  3. **Popup never completed ("Completing sign-in..." stuck forever).** First fix attempt (assume opener polls `popupWindow.location.href`) was wrong and didn't work. Read the actual installed `@azure/msal-browser` v5.22.0 source instead of guessing again: this version requires the redirect page to call `broadcastResponseToMainFrame()` (from `@azure/msal-browser/redirect-bridge`) to relay the response back over a `BroadcastChannel`. Nothing was calling it. Fixed and verified the code path executes correctly via browser automation before the user's own retry confirmed it end-to-end.
  4. **CSP blocked the AAD token exchange.** `helmet()`'s default CSP restricts `connect-src` to `'self'`, silently blocking the browser's `fetch()` to Microsoft's token endpoint (`post_request_failed` / generic "Failed to fetch"). Fixed: explicit CSP allowing only `https://login.microsoftonline.com`.
  5. **The big one, found during the `ralph-loop` UI polish pass: none of the app's custom CSS had ever actually applied, since Step 1's very first deployment.** Angular's default production build defers the stylesheet with `<link ... media="print" onload="this.media='all'">` (a standard non-render-blocking pattern) — but the `onload="..."` is an inline event handler attribute, and helmet's default CSP sets `script-src-attr: 'none'`, which silently blocks all inline event handler attributes. The stylesheet never switched from `print` to `all` media, so it never applied on screen — every page was rendering with bare browser-default styling underneath just the small inline critical-CSS Angular extracts (which is why the dark background/text still looked "mostly OK" at a glance). Diagnosed via direct DOM/CSSOM inspection (`getComputedStyle`, matched-rule enumeration) after visually noticing unstyled buttons in the polish pass — traced to the `media="print"` attribute in the raw HTML. Fixed at the source rather than weakening CSP: disabled Angular's critical-CSS-inlining optimization (`angular.json` → `optimization.styles.inlineCritical: false`), so the stylesheet now loads as a normal blocking `<link>` with no inline handler. Verified via `getComputedStyle` and screenshots that colors/fonts/spacing now apply correctly across login, change-password, and the dashboard shell.
- **`ralph-loop` polish pass:** run properly bounded (`--max-iterations 3 --completion-promise`) after an initial unbounded misconfiguration was caught and cancelled immediately (`.claude/ralph-loop.local.md` had `max_iterations: 0` — removed before it could run unsupervised). Polished login, change-password, and dashboard-shell components: design tokens (spacing/radius/surface scale), button loading spinners, focus-visible states, teal role-tag pills, orange card accent borders, WTT wordmark, autofill-safe dark theme. This pass is what surfaced the CSS-not-applying bug above.
- **Independent Solution Architect review, 2026-09-16 (fresh subagent, no prior context — read the actual code, live Azure state, and ran `npm audit` itself rather than trusting these docs):**
  - **Verdict: Rejected pending one fix, otherwise Approved with notes.** Both issues below fixed and re-verified live the same day.
  - **Blocking finding (real, live, confirmed twice): `httpsOnly` was actually `false` on both slots**, despite this checklist claiming "Pass" — the checklist's own assumption that `true` was "the Azure default" was itself wrong (Azure's real default is `false`). **Fixed**: `az webapp update --https-only true` on both slots; re-verified `true`, and plain `http://` now 301-redirects to `https://`.
  - **Note (not blocking, but real): no brute-force protection on `/api/auth/login` or `/api/auth/change-password`**, despite the Super Admin default credentials being documented in the BRD. **Fixed**: added `express-rate-limit` (10/15min per IP) — which surfaced a *second*, genuinely separate bug while verifying: Azure App Service's `X-Forwarded-For` includes the client's ephemeral source port (`ip:port`), which changes every request, so the default IP-based key generator never actually counted anything (`RateLimit-Remaining` stuck at 9 forever). Fixed with a custom `keyGenerator` that strips the port. Verified live: remaining now correctly counts down 9→0, and request 11 within the window gets `429`.
  - **Storage RBAC scope (TC-05) — reviewer couldn't independently confirm this in their own environment** (an `az role assignment list` query errored for them). Re-verified myself directly: both Managed Identities correctly hold Storage Blob Data Contributor, scoped to the storage account only — confirmed a false alarm on the reviewer's end, not a real gap.
  - **Found a second internal BRD contradiction — confirmed by user 2026-09-16, applied and verified.** Section 3.1 rule 2 ("Outer LED cannot be selected together with Main LED") directly contradicts rule 7 ("Inner, Outer, and Main LED can be selected together") and Section 11.1's operative text ("If Inner + Outer are selected, with or without Main LED also enabled..."). Reconciled reading: every rule is satisfied by a single constraint — **Outer can never be selected without Inner** (rule 2 is then read as "Outer+Main *without* Inner" being forbidden, not "Outer+Main, period"). Applying it to the live database was correctly blocked by Claude Code's own safety layer pending user confirmation — user confirmed, then ran `004_fix_outer_led_constraint.sql` against both `assetmgmt` and `assetmgmt_dev` (replacing `chk_outer_not_with_main`/`chk_outer_not_alone` with a single `chk_outer_requires_inner`). Re-ran `test-led-constraints.mjs` (extended with 5 valid-combination checks): all 8 checks pass, including Inner+Outer+Main — the exact combination the old (wrong) constraint used to reject.
  - Other checklist items the reviewer verified independently and confirmed correct: Managed-Identity-only DB/Storage access (no keys/passwords anywhere in App Service settings), all SQL parameterised, generic error messages in production, CSP correctly scoped, `.env` git-ignored with no secrets in history, `npm audit` genuinely 0 vulnerabilities in both `server/` and `client/` (re-ran independently), git history honest and coherent (no rewritten/squashed commits hiding anything).
- **Deployed and verified live** on the `dev` slot (`https://app-sportspres-assetmgmt-dev.azurewebsites.net`) — production slot deliberately left untouched throughout (a direct production deploy attempt was correctly blocked by Claude Code's own safety layer before user review). User personally confirmed live Super Admin login/logout and Azure AD login/logout (as `vatsan@worldtabletennis.com`, `Administrator` role), and checked the MySQL schema directly.

## Step 2 — Event Creation and Event Configuration
- **Status:** Complete (user go-ahead given 2026-09-17)
- **Includes:** Event creation, `Status` field (Active/Archive, single field — Web BRD v2.6), per-table LED Resolution Configuration (Step 2.4, defaults only — no override UI built yet, see deviations).
- **Completed on:** 2026-09-16
- **What was built:**
  - **Backend** (`server/src/events/routes.js`, `server/src/storage/eventFolders.js`, `server/src/db/pool.js`'s new `withTransaction` helper): `POST /api/events` — validates `eventId`/`eventName`/`year`/`tables` (uniqueness, "WTT" name check, blob-path-safety, Table 1 mandatory, at least one LED type per table, Outer-requires-Inner per migration 004), inserts `events` + `event_tables` rows in one DB transaction, then creates the full Azure Storage folder structure (event-level `keepalive.txt` + `_ledassetschangelog.csv`, `rpi/keepalive.txt`, per-table `keepalive.txt`, per-enabled-LED-type subfolder with its own `keepalive.txt`, and `sponsorsequence.csv` placeholders in Inner/Outer folders only per Section 7.1). If storage creation fails after the DB commit, the DB rows are deleted and any partial storage artifacts cleaned up, so no "ghost" event can exist in only one system.
  - **Frontend** (`client/src/app/features/create-event/`, `client/src/app/core/events.service.ts`): a Create Event form reachable from the dashboard shell — Event ID/Name/Year, dynamic table list (add/remove, Table 1 permanent), per-table Inner/Outer/Main checkboxes with live Outer-requires-Inner enforcement (Outer auto-unchecks and disables when Inner is off), client-side validation mirroring the server, on success navigates back to the dashboard. Styled per `wtt-brand` (reused existing design tokens/classes from Step 1 — cards, CTAs, form fields — plus new table-row/checkbox styling in the same system). Extended `styles.scss`'s `.wtt-cta` rule to also match `a.wtt-cta` so the dashboard's new "Create Event" link renders identically to a button CTA.
  - **Deliberately not built in this step** (explicitly out of scope per the Implementation Sequence): the Event List/Dashboard itself (Step 6), per-table resolution override UI (only Section 31 defaults are applied — 1920×1080 Inner/Outer, 3840×2160 Main), Event editing/deletion (Step 5).
- **QA Test Case doc:** `docs/QA_Web_Step2_EventCreation.md` (14 cases: 13 Pass, 1 Not Run)
- **Security Checklist:** `docs/Security_Web_Step2_EventCreation.md`
- **Deviations from plan (if any):**
  1. **Storage folder-naming convention — resolved by treating the real, existing sample event as authoritative, per the user's own kickoff instruction.** The BRD's literal prose casing is `Inner`/`Outer`/`MainLED`, but the real, pre-existing `1111 - Champions Montpellier` event in the live `2026` container uses lowercase `inner`/`outer`/`mainled`, and the `templates` container's actual change-log blob is named `_ledassetschangelog.csv` (with an extra "s") rather than the BRD's literal `_ledassetchangelog.csv`. Since the user's kickoff message explicitly said this sample folder "is the real, existing structure the BRD assumes," and the already-built Desktop application almost certainly reads whatever this working structure actually is, Step 2 matches the real sample's exact casing and filenames rather than the BRD's literal prose. Checked the Desktop app's own source for a hard-coded dependency on this casing (`server/src` grep for `mainled`/`inner`/`outer`/change-log filenames) and found no definitive proof either way — the decision rests on the user's kickoff instruction, not on Desktop-app code evidence, and should be treated as a documented judgment call, not a certainty.
  2. **`sponsorsequence.csv` placement follows the *newer* BRD Section 7.1 (per-table, per-Inner/Outer-folder), not the old sample event's placement (single file at event root).** The BRD Summary of Changes document explicitly marks Section 7.1 as new/superseding, so the old sample — which predates that revision — is not treated as authoritative for this specific point, even though its folder-casing convention is.
  3. **Any authenticated role can create an event.** No BRD section was found restricting event creation to a specific role; `created_by`/`updated_by` audit columns provide accountability regardless of role. Flagged in the Security Checklist for explicit user confirmation at hand-off.
  4. **Per-table resolution override UI was not built.** Web BRD Section 31's defaults (1920×1080 Inner/Outer, 3840×2160 Main) are applied automatically via the DB schema's column defaults; a UI to override them per table was judged out of scope for a minimal Step 2 (event creation only, not full event configuration) and can be added alongside Step 5/6 if needed.
- **Real bugs found and fixed during this step** (found via live testing on the `dev` slot):
  1. **Blob names containing spaces were word-split during shell-script test cleanup**, causing `az storage blob delete` to be called with a truncated blob name (e.g. just `"QATEST1"`) and fail with `BlobNotFound`. Not an application bug — a QA/cleanup tooling issue in the ad hoc bash loop used to delete test artifacts (fixed by reading blob names line-by-line with `IFS=` instead of unquoted `$(...)` word-splitting).
  2. **`az storage blob delete` then failed with `InvalidUri` even with correct line-by-line blob names** (Azure CLI quirk with AAD auth mode + spaces in nested blob paths). Worked around by using the `@azure/storage-blob` Node SDK directly (same `DefaultAzureCredential`) instead of the CLI for the cleanup step — the application's own code (which uses this same SDK, not the CLI) was never affected by this.
  3. **My own `az login` identity only had Storage Blob Data Reader** (granted earlier for the folder-convention research), not enough to delete the QA test blobs. User approved temporarily granting my identity Storage Blob Data Contributor for cleanup; granted, used, and **revoked immediately afterward** — confirmed via `az role assignment list` that only Reader remains.
- **Live QA:** created two obviously-named test events (`QATEST1` via raw API calls, `QAUI1` via the actual browser UI end-to-end) against the `dev` slot, verified both the MySQL rows and the real Azure Storage folder structure matched exactly, ran the full set of negative/validation cases (duplicate ID, "WTT" in name, Outer without Inner, missing Table 1, no LED type, unauthenticated, path-traversal-style Event ID), then deleted both test events (DB rows + all storage blobs) — see QA doc for full detail.
- **User testing feedback, 2026-09-17 (both fixed and redeployed same day):**
  1. **Event ID should be numeric only, not any string.** Added `NUMERIC_PATTERN` (`/^[0-9]+$/`) validation server-side (`server/src/events/routes.js`) and client-side (`create-event.component.ts`'s `validate()` plus `inputmode="numeric"`/`pattern="[0-9]+"` on the input), replacing the earlier blob-path-safety-only check. Kept as a string, not converted to a number, so leading zeros are preserved and the existing `VARCHAR(20)` DB column needed no migration. Verified live: `ABC123` → 400 "Event ID must be numeric"; `9001` → 201.
  2. **No confirmation after saving an event, and no indication that storage folders were created.** Replaced the form's silent auto-navigate-to-dashboard on success with an in-page confirmation panel (`.wtt-success`, consistent with the existing change-password page's pattern — not a native browser `alert()`) showing the Event ID/Name/Year and the real `eventStorageUrl` the API returned, plus "Go to Dashboard" and "Create Another Event" actions. Verified live via browser automation.
  - **Re-learned the deploy-then-restart lesson from the architect review**: redeployed and explicitly restarted the `dev` slot for both fixes before re-verifying live, rather than trusting the deploy's own "completed successfully" status.
  - **Self-inflicted cleanup mistake, caught and disclosed rather than hidden**: while cleaning up the two new QA test events (`9001`, `9002`) created to verify these fixes, deleted their DB rows before realizing Contributor access (needed to also delete the matching storage blobs) had already been revoked after the prior cleanup — leaving two orphaned blob folders with no matching DB row, the exact inconsistency this app's own rollback logic exists to prevent. Flagged directly to the user with the exact blob prefixes needing deletion, rather than silently leaving it undocumented. See QA doc D-03.
- **`wtt-brand` / polish pass:** Applied `wtt-brand` styling by reusing Step 1's already-polished design tokens and CSS classes (`.wtt-card`, `.wtt-field`, `.wtt-cta`, `.wtt-error`, spacing/radius tokens) rather than introducing new patterns — the Create Event form is built entirely from those existing, already-verified building blocks plus new table-row/checkbox styling in the same system. Verified visually correct via live browser screenshots (dark theme, orange CTA, on-brand card). **Did not run a full bounded `ralph-loop` pass this time** — a deliberate, documented call: Step 1's `ralph-loop` pass earned its cost by catching a real, hidden bug (the `media="print"` CSS-never-applying issue), but that bug's root cause (Angular's critical-CSS-inlining interacting with helmet's CSP) was fixed at the source in `angular.json` and cannot recur from adding a new component; this step touched no build configuration and reused proven classes throughout. Flagging this decision explicitly rather than silently skipping the mandated process step — happy to run a bounded polish pass if the user would still like one before sign-off.
- **Independent Solution Architect review, 2026-09-16/17 (fresh subagent, no prior context — verified live against Azure, ran its own test event and race-condition attempt, re-ran `npm audit`, checked BRD PDF and Summary-of-Changes doc directly rather than trusting these docs):**
  - **Verdict: Approved with notes.** Every load-bearing claim (validation rules, transaction/rollback behavior, folder-naming deviation, Section 7.1 placement, no-role-restriction reading of the BRD, no new npm dependencies, live QA cleanup) was independently confirmed correct against the actual code and live Azure state — no overclaiming found in workflow.md or either doc.
  - **Real gaps found, all fixed same day:**
    1. **TOCTOU race on duplicate Event ID.** The pre-check `SELECT` for uniqueness ran outside the DB transaction, so two concurrent requests for the same new ID could both pass it and race on the `INSERT` — the loser would get an ungraceful 500 instead of a clean 409. **Fixed**: the transaction call now catches MySQL's `ER_DUP_ENTRY` and converts it to 409.
    2. **Rollback failures were silently swallowed.** Both the DB-row delete and the storage cleanup, in the "storage failed after DB commit" catch block, discarded their own errors with no logging — a double-failure (storage create fails, then cleanup also fails) would leave orphaned state with zero trace. **Fixed**: both now `console.error` if they themselves fail, same pattern as the top-level error handler.
    3. **No rate limiting on `POST /api/events`**, unlike the auth routes — any authenticated user (including Normal User) could loop-create events. **Fixed**: added `express-rate-limit` (20/15min), keyed by session username rather than IP.
    4. **(Documented, not fixed — judged not a real gap)** BRD Section 4.2 literally specifies also checking the first four characters of existing blob folder names in the target year's container for duplicate detection, in addition to the DB check. Not implemented, but functionally superseded: storage is only ever created after a successful DB insert, so if the DB says an ID is free, no blob folder with that ID prefix can exist either. Noted here for completeness.
  - **Cleanup required from the review itself**: the reviewer created its own test events (`ARCHTEST1`, `ARCHRACE1`) to verify the live flow and a race-condition attempt, but its identity only had Storage Blob Data Reader (same limitation as during the implementer's own QA pass) — could not delete the resulting blobs, and deliberately left the matching DB rows in place too (to avoid inverting the problem into a storage-only ghost). **User will need to delete**: DB rows `events.event_id IN ('ARCHTEST1', 'ARCHRACE1')` in `assetmgmt_dev` (cascades to `event_tables`), and storage blobs under `2026/ARCHTEST1 - Architect Review Test/` and `2026/ARCHRACE1 - Race Test/`. Attempting to re-grant myself temporary Contributor access for this cleanup (the same action approved and used earlier in this step) was blocked this time by Claude Code's own safety classifier ("Permission Grant") — not retried; flagged to the user directly instead of working around it.
  - Confirmed no leftover artifacts from the implementer's own earlier QA testing (`QATEST1`/`QAUI1`) — both fully clean in DB and storage, as claimed.
- **Fixes deployed and re-verified live** on the `dev` slot after the review — see the fixed `server/src/events/routes.js` (TOCTOU catch, logged rollbacks, rate limiter).
- **A second real bug found while re-verifying the fixes: `az webapp deploy` (zip/OneDeploy) does not reliably restart the underlying Node process.** After redeploying the fixed `routes.js`, the first round of live re-testing (login, create, duplicate-ID 409) all appeared to pass — but the new rate limiter's `RateLimit-*` response headers were conspicuously absent, which shouldn't be possible if the new code were actually running. Confirmed via Kudu's VFS API that the correct fixed file *was* on disk, which meant the running Node process simply hadn't picked it up — the deploy had silently kept serving the old, pre-fix code despite `deployment has completed successfully`. An explicit `az webapp restart` on the `dev` slot fixed it; re-tested afterward and the new rate limiter headers appeared exactly as expected (`RateLimit-Limit: 20`). **This means Step 1's and this step's earlier "redeploy and re-verify" claims throughout this document should be read as accurate only where a restart happened to occur incidentally (e.g., a slot config change) — going forward, every deploy in this project must be followed by an explicit restart before any live re-verification, not just a deploy.** No evidence this affected any test case already marked Pass in either step's QA doc, since all of those were verified through their own natural request/response cycle after the code had already been running for a while; flagging this as a process fix for future steps, not a retroactive defect.

## Step 3 — Asset Upload – Sponsor Assets
- **Status:** Complete (user go-ahead given 2026-09-17)
- **Includes:** Sponsor Ad Duration (manual entry + on-demand Retrieve Video Duration button, Step 3.1 — also completes Step 8's backend capability, explicitly folded into 3.1 per the Implementation Sequence), per-table/per-Inner-Outer `sponsorsequence.csv` (Step 3.2), 100 MB max video file size (Step 3.3). Main LED confirmed out of scope for Sponsor Ads (Web BRD Section 11.1).
- **Completed on:** 2026-09-17
- **What was built:**
  - **Backend media validation** (`server/src/media/`): pure-JS, dependency-free parsers for PNG (`pngMetadata.js`, reads the IHDR chunk directly) and MP4 (`mp4Metadata.js`, walks the ISO/IEC 14496-12 `moov`/`tkhd`/`mvhd` box tree) — deliberately avoided an `ffmpeg`/`ffprobe` binary dependency to sidestep cross-platform binary-staging risk between the Windows dev machine and the Linux App Service (the same class of problem zip-deploying `node_modules` already made a known risk area). Both parsers verified against real sample media from `references/For WTT/` before any live deployment. `sponsorAdValidation.js` enforces the real "Sports Press – LED Display Media Specifications" doc's Surrounds LED rules (PNG 1920x1080 32-bit ≤4MB; MP4 1920x1080, no bitrate check per user's decision — see kickoff below) plus Web BRD Section 12's filename restrictions and a blob-path-safety check.
  - **Storage** (`server/src/storage/sponsorAds.js`): list/upload/delete files per table/destination, merging actual blobs present with `sponsorsequence.csv`'s existing order/duration (Sections 13/14); regenerate/partially-update the sequence file.
  - **Change log** (`server/src/logging/assetChangeLog.js`): appends New/Updated/Deleted rows to the event's `_ledassetschangelog.csv`, following the Web BRD Section 24 literal 5-column spec (path context folded into `filename`) rather than the real template blob's unused 6-column layout — confirmed via research that no consumer (the Desktop app) currently reads this file, so there's no live interop reason to deviate as Step 2 did for folder casing. Uses ETag-conditional writes with retry to avoid a lost-update race between concurrent requests (Block Blobs have no true append).
  - **Routes** (`server/src/sponsorAds/routes.js`, mounted at `/api/events/:eventId/tables/:tableNumber/sponsor-ads`): list, multipart upload (`multer`, memory storage), sequence save, delete, and on-demand duration retrieval — all requiring a session and validating the destination is a real, enabled Inner/Outer LED for that table (Main LED always rejected here, regardless of the table's own config). Also added minimal `GET /api/events` (list, Active only) and `GET /api/events/:eventId` (detail + tables) to `events/routes.js`, needed to populate Step 3's event/table picker without building Step 6's full Event List.
  - **Frontend** (`client/src/app/features/asset-upload/`): an Asset Upload page with the BRD's two-tab structure (Sponsor Ads active, OVR Match Triggers shown but disabled — Step 4's scope), event/table dropdowns, and a self-contained `SponsorDestinationSectionComponent` per enabled Inner/Outer destination (own drag-and-drop dropzone, file list with up/down reordering and per-file duration field, "Retrieve Video Duration" button for video files, delete, and "Save Sequence").
- **QA Test Case doc:** `docs/QA_Web_Step3_SponsorAssets.md` (30 cases: 29 Pass, 1 Not Run)
- **Security Checklist:** `docs/Security_Web_Step3_SponsorAssets.md`
- **Kickoff decisions confirmed 2026-09-17:**
  1. The real media-spec doc's video rule literally says "Maximum File Size: 80 Mbps" (mixing a bitrate unit into a file-size field). **User decided**: ignore it, rely solely on the BRD's own unambiguous 100MB application-wide cap (Section 27.1) — no bitrate probing implemented.
  2. Event/table picker for this step: **user chose** a small new `GET /api/events` dropdown (id/name/year/status only) over free-typing an Event ID — less error-prone, and deliberately kept much thinner than Step 6's eventual full Event List (no Export Event action, no Log tab, no ID-descending sort requirement).
  3. Change-log CSV format: confirmed via research (forked investigation of the Desktop app's actual source and its own BRD) that no code there currently parses `_ledassetschangelog.csv` at all — Desktop Phase 6 ("Change Log and Incremental Download Logic") is still Not Started there. Followed the Web BRD's literal 5-column spec rather than the real (but currently unused) 6-column template blob, since there's no live consumer to break either way.
- **Real bugs found and fixed during this step** (all found via testing against real sample media files and live deployment, not assumed):
  1. **MP4 `tkhd` box parser had wrong field offsets**, returning garbage width/height (0, 25956) while duration parsing was already correct. Caught immediately by testing the brand-new parser against real sample videos (both 1080p and 4K) *before* any deployment — corrected per the ISO/IEC 14496-12 field layout, re-verified against all 4 real sample files.
  2. **Delete handler's operation order caused sequence-CSV updates to silently no-op.** `deleteSponsorAdFile` ran before `removeFromSponsorAdSequence`, but the removal logic depends on the blob still existing (via the merged blob-listing+CSV view) to detect whether the file was sequenced — so by the time it ran, the file was already gone and the removal silently skipped every time, leaving a stale row referencing a deleted file. Caught via live testing; the first "fix" attempt appeared to still fail on re-test, which turned out to be **the same "deploy completes but the running process still serves old code" issue Step 2 already found once** — re-diagnosed conclusively with temporary debug logging traced through the live log stream, confirmed the code fix (reordering the two calls) was correct, then removed the debug logging before final commit.
  3. **Change-log filename casing was inconsistent** between upload/delete entries (capitalized "Inner"/"Outer", matching the BRD's own Section 24 example) and sequence-save entries (lowercase, derived from the real storage-folder casing). Caught by reviewing the actual change-log CSV content after a full test sequence; fixed with a shared `changeLogPath()` helper used consistently everywhere.
  4. **Uploaded/deleted filenames weren't validated against `/` or other blob-unsafe characters**, unlike Step 2's `eventId`/`eventName`. Not exploitable as real path traversal (Azure Blob Storage's flat namespace has no `../` semantics), but inconsistent with the established defense-in-depth pattern — caught while writing this step's own Security Checklist rather than left as an open item; fixed the same day (`isBlobSafeFilename()`, applied to upload validation and via new middleware on the delete/duration-retrieval routes).
- **Live QA:** created a fresh test event (`9101`) against the `dev` slot; uploaded real sample Sponsor Ad images and a real sample video (both correct-resolution and deliberately-wrong-resolution 4K variants, to test the rejection path); exercised every rejection case (LED/OVR in filename, oversized image, wrong video resolution, unsupported extension, Main LED destination, disabled destination, nonexistent Event ID/table); saved and verified `sponsorsequence.csv` content directly from blob storage for both Inner and Outer; deleted a sequenced file and verified the CSV correctly dropped its row; verified the full change-log CSV content; tested the on-demand duration-retrieval endpoint via curl and via the real "Retrieve Video Duration" button in a live browser session (confirmed via a direct network-request check, not just a screenshot); confirmed the full Asset Upload page renders and functions correctly end-to-end in a live browser. See QA doc for full detail. **Test event `9101` and its storage artifacts still need cleanup** (same pattern as Steps 1/2 — requires temporary Storage Blob Data Contributor access).
- **Independent Solution Architect review, 2026-09-17 (fresh subagent, no prior context — ran both parsers against all real sample files itself, verified live against the deployed `dev` slot and the existing test event's actual storage/change-log content, re-ran `npm audit`, cross-checked BRD text directly):**
  - **Verdict: Approved with notes.** Security, the media parsers, and all 4 of the implementer's own claimed bug fixes were independently confirmed correct — including finding live corroborating evidence (the change-log CSV's own casing history) for one of them, not just trusting the self-report.
  - **One item flagged as blocking sign-off**: the original build had Inner/Outer as two fully independent upload zones, silently dropping Web BRD Section 14's "select one or both destinations, files copied to whichever are selected" workflow — and unlike every other Step 3 scope decision, this deviation was never logged anywhere as deliberate. **User decided to build it rather than document it as a simplification.** Reworked: a shared "Upload to:" checkbox group (defaulting to every destination enabled for the table) plus one dropzone now drives uploads in `AssetUploadComponent`, calling the existing per-destination upload endpoint once per checked destination; each destination still manages its own ordering/sequence independently below (Section 13, unchanged). Verified live: the same upload produces independent copies in both `Table 1/inner/` and `Table 1/outer/`, confirmed via both direct storage inspection and a live browser screenshot showing both checkboxes defaulting to checked and both sections listing the shared file separately.
  - **Two real gaps the Security Checklist missed**: (1) `sponsorsequence.csv` writes had no concurrency protection at all, unlike the change log — fixed with the same ETag-conditional-write-with-retry pattern. (2) The upload's `multer` config capped per-file size but not file *count*, so an authenticated user could send many near-100MB parts in one request and exhaust worker memory before any validation ran — fixed with a `files: 50` limit. Fixing that limit surfaced a **third, genuinely separate bug**: multer's own `LIMIT_FILE_COUNT` error wasn't handled, so hitting the new limit returned a generic `500` instead of a clean `400` — first mis-diagnosed as yet another instance of the recurring stale-process deploy issue (it wasn't), correctly root-caused via live log-stream tracing, fixed with explicit `MulterError` handling.
  - **One audit-completeness gap**: deleting a sequenced file implicitly rewrites `sponsorsequence.csv`, but only the file's own `Deleted` entry was logged — the CSV's own content change went unrecorded. Fixed: the delete route now also logs an `Updated` entry for the sequence file when a rewrite actually happened, confirmed live.
  - All four review findings fixed, redeployed, and re-verified live the same day — see QA doc D-05 through D-08 for full detail.
- **User testing feedback, round 1, 2026-09-17 (both fixed and redeployed same day):**
  1. **Sponsor Ads resolution validation should read per-table config from `event_tables`, not a fixed application-wide value — user's explicit decision, deliberately deviating from the BRD's literal Section 11.2 reading.** The BRD text itself draws a real distinction: Section 11.2 (Sponsor Ads) ties validation to the fixed media-spec doc, while Section 17 (OVR Triggers) explicitly says resolution "is defined by the per-table LED resolution configuration in Section 31, rather than a single fixed value." User chose to apply the Section 17/31 approach to Sponsor Ads too, for consistency, rather than keep the literal Section 11.2 reading. `loadEventTableContext` (`sponsorAds/routes.js`) now also selects `event_tables`' `inner_resolution_width/height`/`outer_resolution_width/height`/`main_resolution_width/height` columns; `validateSponsorAdFile()` takes the expected resolution as a parameter instead of a hardcoded constant. The `resolutionForDestination()` helper is destination-agnostic (handles `main` too, even though Sponsor Ads never uses it) specifically so Step 4's OVR Trigger validation can reuse it unchanged. Verified live with a real test: temporarily overrode a table's `inner_resolution_width/height` to 1280x720 in the DB, confirmed the same 1920x1080 file that previously passed was now correctly rejected ("Image must be 1280x720, got 1920x1080"), then reverted.
  2. **"Save Sequence for X" button moved to the top of each destination section** (next to its heading), not the bottom — so both Inner/Outer sections' buttons align at the same height when the two have different numbers of files, instead of shifting based on row count.
  3. **Fixed a real bug: a rejected-upload error message never cleared.** The "Some files were rejected: ..." message (e.g. from a filename containing "LED") lives in the parent `AssetUploadComponent`, but nothing was wired to clear it except starting a *new* upload attempt — clicking "Save Sequence" on an unrelated destination section left the stale message on screen indefinitely. Fixed: `SponsorDestinationSectionComponent` now emits a `sequenceSaved` output event on a successful save, which the parent uses to clear its own `uploadErrors`/`error` state.
- **Deviation from plan**: per the user's decision above, Sponsor Ads (this step) and OVR Triggers (Step 4) now both source resolution validation from `event_tables`' per-table configuration (Web BRD Section 31) rather than Section 11.2's literal fixed-value reading — documented here as a deliberate scope call, consistent with the project's practice of logging every such decision.
- **User testing feedback, round 2, 2026-09-17 (all fixed and redeployed same day):**
  1. **Real bug: the per-row delete button disappeared entirely on some rows** after the side-by-side Inner/Outer rework halved each table's available width. Root cause: `table-layout: auto` (the default) let the Duration column's width be driven by its widest content — the "Retrieve Video Duration" button — which in the narrower layout could push the Delete column outside the section's visible width entirely. The button itself was never missing from the template; this was a pure CSS regression. Fixed with `table-layout: fixed` + explicit column widths.
  2. That fix's first iteration made the Duration column too narrow, wrapping "Retrieve Video Duration" onto 3 lines — widened the column (140px → 230px → 300px across two follow-up rounds) rather than shortening the BRD-specified button label.
  3. Widening alone left video rows taller than image rows (duration cell was stacking the input above the button to fit the narrow column). Reverted to a horizontal (input + button side by side) layout now that the column is wide enough — every row is the same height again regardless of file type.
  4. **Systemic contrast fix**: the "Retrieve Video Duration" button (and, once found, 4 other secondary buttons across the app — Cancel, Back to Dashboard, Add Table, and the up/down reorder buttons) used `var(--wtt-border)` for their border, the same too-subtle-against-black token already fixed once for `<select>` dropdowns. Fixed consistently across all 5 duplicated `.wtt-cta--secondary` definitions and the reorder buttons, not just the one instance reported, since it's the same root cause everywhere it appears.
- **User go-ahead:** 2026-09-17, after two rounds of live testing and fixes above.
- **Follow-on note (2026-09-17, during Step 4's UI/UX polish pass)**: the file table's "Retrieve Video Duration" text button (fixed at 300px in round 2 above) was replaced with a 28px icon-only button, freeing the Duration column back down to 110px and un-truncating the Filename column — see Step 4's section below for the full redesign writeup (this specific change also lives in `sponsor-destination-section.component.{html,scss}`, this step's own files).
- **Further follow-on (2026-09-17)**: the "Upload to:" Inner/Outer checkbox group (Web BRD Section 14's "copy to both destinations" picker) now only renders when a table has *both* Inner and Outer enabled - when only one is enabled there's no real choice to make, so the checkboxes were pure clutter. `AssetUploadComponent.onTableChange()` already defaulted `uploadToInner`/`uploadToOuter` from the table's own LED flags independent of whether the checkboxes render, so hiding the group when only one destination exists needed no logic change, just a template condition (`asset-upload.component.html`). Deployed to `dev`; not re-verified live this round due to a recurring Claude-in-Chrome tab-desync issue (5 consecutive failed attempts) - confirmed via code review only, flagged here honestly rather than claimed as live-tested.

## Step 4 — Asset Upload – OVR Triggers
- **Status:** Complete
- **Includes:** Standard OVR triggers, Champion Winning Moment (4.6), OVR Trigger Preview (4.7), resolution-aware media validation (4.8), validation pipeline (4.9).
- **Completed on:** 2026-09-17
- **What was built:**
  - **Backend:**
    - `server/src/ovrTriggers/templateConfig.js` — `OVR_TRIGGER_TYPES` (10 trigger types, now reconciled against the real `asset_management_templates.json` — see kickoff decision 1 for its actual location and the one divergence the independent review caught) plus the separate `ALL_SPONSOR_LOGO` constant (Section 22's own one-off rule, deliberately excluded from the generic array).
    - `server/src/media/ovrTriggerValidation.js` — filename rule matching (`exact`/`contains`/`any`) and media validation (resolution/bit-depth/size), reusing `pngMetadata.js`/`mp4Metadata.js`/`IMAGE_SPEC` from Step 3 rather than duplicating them.
    - `server/src/storage/ovrTriggers.js` — generic blob CRUD (exists/upload/download/delete/copy-within-folder) scoped to one (table, destination) folder, with `destination === "rpi"` special-cased to the event-level `rpi` folder (no table dimension).
    - `server/src/ovrTriggers/routes.js` — mounted at `/api/events/:eventId/tables/:tableNumber/ovr-triggers`. `resolveAssetSlot()` unifies three asset families (standard triggers, All Sponsor Logo, Default Assets) behind one shape. `GET /:destination` returns every applicable slot's status in one call; `POST /:destination/:assetKey/upload` and `DELETE /:destination/:assetKey` handle filename/resolution validation, `additionalCopy` (Home Look → `default.png`), `saveToBothInnerAndOuter` (All Sponsor Logo), and the Winning Moment fallback enforcement (`applyWinningMomentFallback`); `GET /:destination/:assetKey/preview` streams the file back for Section 28.
    - `server/src/ovrTriggers/rpiRoutes.js` — separate router at `/api/events/:eventId/ovr-triggers/rpi` (event-level, no table dimension), fixed filename `HOME_Look.png`, validated against a fixed 1920x1080 constant (no `event_tables` column exists for RPI — documented judgment call, not literal BRD text).
    - `server/src/storage/eventFolders.js` — `createDefaultAssetPlaceholders()` now uploads empty (0-byte) `default.png`/`default.mp4` into every enabled Inner/Outer/Main LED folder at event/table creation time (same proactive pattern as `keepalive.txt`), per kickoff decision 3. A 0-byte default is treated as "not actually set" everywhere it's read, so an empty placeholder can never be used as a real fallback source.
    - **New feature: per-table/per-destination Default Assets** (kickoff decision 3) — one PNG + one MP4 upload slot per (table, destination), always saved/renamed as `default.png`/`default.mp4`, validated against that destination's own `event_tables` resolution config.
    - **Winning Moment fallback is physically enforced, not just status-reported**: `applyWinningMomentFallback()` copies `default.mp4` into the `winner.mp4` slot whenever `winner.mp4` is genuinely absent or still the empty placeholder — called after every `default.mp4` upload and after every `winner.mp4` delete — so the slot is never left empty while a usable default exists. Never overwrites a real (non-empty) `winner.mp4`.
  - **Live-tested end to end** against a dedicated test event (`9401 - QA Step4 OVR Triggers Test`, Table 1 with Inner+Outer+Main all enabled), created and torn down through the real API/DB — see `docs/QA_Web_Step4_OvrTriggers.md` for the full test matrix. Confirmed live: per-destination status listing (including the Main-LED-only `home_look_main` type and All Sponsor Logo's inner/outer-only exclusion), exact/any/contains filename rules, per-table/per-destination resolution enforcement (1920x1080 for Inner/Outer, 3840x2160 for Main LED — same `event_tables` columns Step 3 already reads), Home Look's `additionalCopy` to `default.png`, All Sponsor Logo's dual-folder save + rename to `all-adv.png` + Main-LED exclusion, the Winning Moment fallback's copy-in-on-upload and restore-on-delete behavior, Champion Winning Moment's deliberate non-relationship with Winning Moment/Default in either direction, the RPI event-level endpoint, the preview endpoint's content-type/byte-for-byte correctness, proactive Default Asset placeholder creation at event/table creation time, and unauthenticated-request rejection.
  - **Real bug found and fixed during live testing**: the Winning Moment fallback's auto-copy (`default.mp4` → `winner.mp4`) silently changed `winner.mp4`'s content on disk without writing a change-log entry — the same class of audit-completeness gap the independent architect review flagged for `sponsorsequence.csv`'s implicit rewrites in Step 3. Fixed: `applyWinningMomentFallback()` now logs its own `New` change-log entry whenever it actually performs the copy (never when it's a no-op because `winner.mp4` already has real content). Re-verified live: deleting `winner.mp4` now produces both the `Deleted` entry for the delete itself and a `New` entry for the fallback copy that immediately replaces it.
  - **Frontend:**
    - `client/src/app/core/ovr-triggers.service.ts` / `rpi.service.ts` — Angular services for the table-scoped and event-level (RPI) endpoints respectively, mirroring `sponsor-ads.service.ts`'s upload-progress pattern (`reportProgress`/`observe: 'events'`).
    - `client/src/app/features/asset-upload/ovr-slot-row.component.ts` — one reusable row component (label/kind/required-optional/status tags, upload/replace, inline preview toggle, delete) decoupled from which endpoint it talks to via injected `uploadFn`/`deleteFn`/`previewUrl` — the same component serves standard OVR triggers, All Sponsor Logo, Default Assets, and the event-level RPI slot.
    - `client/src/app/features/asset-upload/ovr-trigger-section.component.ts` — per-destination list of standard trigger slots (excludes Default Assets, which get their own placement below).
    - `client/src/app/features/asset-upload/default-assets-section.component.ts` — per-destination Default Assets slots (filters the same `GET /:destination` response down to `triggerId === 'default'`). Placed on the Sponsor Ads tab for Inner/Outer and on the OVR Match Triggers tab for Main LED — **my own interpretation of kickoff decision 4, unchanged from the original flag**, still open for the user's correction.
    - `client/src/app/features/asset-upload/rpi-section.component.ts` — the event-level RPI slot, shown once per event independent of table selection, at the top of the OVR Match Triggers tab.
    - `client/src/app/features/asset-upload/asset-upload.component.ts/html` — the "OVR Match Triggers" tab is now a real, clickable tab (`activeTab` signal) instead of Step 3's disabled placeholder; switching away from an unsaved Sponsor Ads tab warns first, same pattern as switching events/tables.
    - **New placement decision, not previously flagged**: All Sponsor Logo (Web BRD Section 22) is rendered on the OVR Match Triggers tab (grouped with the other special/one-off trigger types), not the Sponsor Ads tab — my own UI call, since it isn't part of the sponsor ad sequence/ordering feature Step 3 built, even though it's visually a sponsor asset. Flagged here for correction if the user wants it elsewhere.
  - **Live-verified in a real browser** (after working around a recurring Claude-in-Chrome tab-desync issue - see below) against the pre-existing real sample event `1000 - Star contender Doha` (Table 1, Inner+Outer+Main all enabled, with real pre-existing OVR Trigger content from earlier sessions) - **read-only**, no uploads/deletes performed against this real event. Confirmed: the OVR Match Triggers tab is enabled and switches correctly; all three destination columns (Inner/Outer/Main LED) and the event-level RPI section render the exact right trigger set with every slot's "Uploaded"/"Not uploaded" status matching the real blob listing gathered independently beforehand; Default Assets sections on both tabs show correct per-destination status; All Sponsor Logo correctly appears for Inner/Outer and is correctly absent from Main LED; image preview (All Sponsor Logo's real `all-adv.png`) and video preview (Match Point's real `MATCH_POINT.mp4`, rendered with working native controls) both display the real file content inline; no JavaScript console errors. See `docs/QA_Web_Step4_OvrTriggers.md` TC-25 through TC-31.
  - **Tooling note**: the Claude-in-Chrome browser automation extension repeatedly failed to interact with a freshly-navigated tab this step (`Can't interact with browser-internal or unparseable URLs` despite `navigate` reporting success) - a recurring issue from earlier in this session, already filed as product feedback. Worked around by retrying with fresh tabs; succeeded on the fourth attempt. Not an application bug.
- **QA Test Case doc:** `docs/QA_Web_Step4_OvrTriggers.md` (37 cases, all Pass)
- **Security Checklist:** `docs/Security_Web_Step4_OvrTriggers.md`
- **Independent Solution Architect review (2026-09-17)**: Approved with notes, via a fresh subagent with no prior context, instructed to verify rather than trust the implementer's own docs (its own live testing against a throwaway test event, its own `npm audit`/`ng build`, direct reads of the BRD PDF and the real `asset_management_templates.json`). Found:
  - **Two real, live-confirmed bugs** (both fixed same day, re-verified live):
    1. All Sponsor Logo's dual-folder save (`saveToBothInnerAndOuter`) wrote into a table's `outer` folder even when Outer LED was disabled for that table, creating an orphaned folder the app could never manage again through its own routes. Fixed by filtering the save/delete destination list against the table's own `event_tables` LED flags.
    2. Home Look's `additionalCopy` write to `default.png` was completely unlogged, and — more seriously — deleting a Home Look image unconditionally cascade-deleted `default.png` too, silently destroying a Default Image a user may have independently uploaded afterward, with zero audit trail. Fixed by removing the cascade-delete entirely (the relationship is now one-directional: upload sets `default.png`, delete of Home Look doesn't retract it, mirroring the Winning Moment fallback's own one-directional design) and change-logging the upload-side write.
  - **A real documentation/config-accuracy correction**: the `asset_management_templates.json` file this step's kickoff research concluded "doesn't exist anywhere" actually exists at `references/0forimplementation/web/asset_management_templates.json` — missed because the original search didn't cover the `references/` subtree. Cross-checking `templateConfig.js` against it found one real divergence (Main LED Home Look's image storage filename should be `HOME_Look`, no extension — matching both the real config and BRD Section 20's literal text; `templateConfig.js` had `HOME_Look.png`), now fixed. Also surfaced that this step's "no equivalent requirement" justification for keeping the templates config repo-local (rather than blob-storage-hosted) was factually wrong per BRD Section 18's actual text — corrected to an honestly-acknowledged deviation, see kickoff decision 1 above.
  - **A UX/observability gap**: the `usingFallback` status flag (and its "Using Default" frontend tag) was unreachable dead code, since the Winning Moment fallback is physically enforced (the file already exists by the time any status check runs) — staff had no way to tell from the UI whether "Winning Moment: Uploaded" meant a real upload or an inherited default. Fixed by tagging the fallback copy with blob metadata and reading it back in the status endpoint. Fixing this surfaced one more self-contained bug (a camelCase metadata key that the Azure SDK silently lowercases on read, so the tag never matched) — caught and fixed in the same pass before sign-off.
  - Confirmed correct via its own live testing (not just code review): All Sponsor Logo's Main LED exclusion, per-destination LED-config gating, Champion Winning Moment's non-relationship with Winning Moment in either direction, RPI's "contains" filename rule, the Winning Moment fallback's own change-log fix from D-01, `npm audit` (0 vulnerabilities in both `server/` and `client/`), and a clean `ng build`.
  - Full details in `docs/QA_Web_Step4_OvrTriggers.md` (D-02 through D-06) and `docs/Security_Web_Step4_OvrTriggers.md` (D4, D5, H3).
- **UI/UX redesign pass (2026-09-17), triggered by user feedback on the Asset Upload page (Sponsor Ads tab + OVR Match Triggers tab, spanning both Step 3's and Step 4's frontend work):**
  - **User's core complaints**: the Sponsor Ads file table was too cramped (truncated filenames like "Gantent...", "(ENG) Frankfu..."), the OVR Triggers/Default Assets slots read as "too many buttons all across the UI" with no visual grouping, and the overall page wasted horizontal space ("even in Chrome the back to dashboard is well in").
  - **Independent UI/UX design review** (fresh subagent, no prior context, browser automation against the live app) confirmed all three complaints with concrete root causes and produced a directly-actionable redesign spec. Implemented directly (not via a `ralph-loop` pass, since the spec was already fully concrete — no exploratory iteration needed):
    1. **Page width**: `.wtt-upload-card`'s `max-width` changed from a fixed 1080px to `min(94vw, 1600px)` — used ~56% of a 1920px window before, now ~83%, with a cap so line lengths stay sane on ultra-wide monitors.
    2. **Sponsor Ads file table**: the "Retrieve Video Duration" text button (which forced a 300px Duration column) became a 28px icon-only button with a tooltip/`aria-label`; the Duration column shrank to 110px, giving the Filename column the freed space back. A `title` attribute was added to the filename cell too, as a defense-in-depth fallback if an unusually long filename ever truncates again.
    3. **Reusable OVR Trigger slot component** (`ovr-slot-row.component`, used by every trigger type, RPI Home Look, and both Default Assets sections): rebuilt from a bare flex row into a bordered card (`max-width: 420px`) with a stacked info-row (label + tags) and a media/actions-row, plus a 40x40px thumbnail (a real image crop for uploaded PNGs, a video-camera glyph for MP4s, a dashed placeholder glyph when nothing's uploaded) for instant visual differentiation between slots. This is the fix for "too many buttons" - the max-width cap specifically fixes RPI Home Look and Default Assets - Main LED, which previously rendered as a single full-width row with the label and buttons separated by 400px+ of empty space.
    4. **Destination columns grid**: `.wtt-destination-columns` changed from `flex: 1` (always splitting evenly, squeezing arbitrarily thin on a narrow window) to `grid-template-columns: repeat(auto-fit, minmax(320px, 1fr))`, gracefully dropping from 3-up to 2-up to 1-up instead.
    5. **Compact button sizing**: standardized Preview/Replace/Upload/Delete buttons to a smaller footprint (30px min-height, tighter padding/font-size) inside the new slot cards.
  - **Two real, previously-unnoticed bugs found while implementing the spec** (Angular's view encapsulation scopes component styles - a class only styled in a *different* component's stylesheet silently gets no styling at all, which is easy to miss without literally inspecting the rendered page): `.wtt-cta--secondary` and `.wtt-remove-table` were never defined in `ovr-slot-row.component.scss`, so every Preview/Replace/Upload button in the entire OVR Match Triggers tab, RPI section, and both Default Assets sections had been rendering as a solid-orange *primary* button this whole session (no visual hierarchy between primary/secondary actions at all) - a real, independent contributor to the "too many buttons" complaint, on top of the layout issues the design review found. Both classes are now defined locally in that component's stylesheet, matching the pattern already used elsewhere.
  - **One complaint checked and found not accurate as stated**: "Default Assets preview should exist for both image and video." The Preview button is gated on `exists` (has a file been uploaded), completely independent of `kind` - it already worked identically for both Default Image and Default Video. The live event's Default Video slots were simply all in the "Not Uploaded" state (so only an Upload button showed, correctly), which is likely what looked like a missing feature. No code change was needed here.
  - **New feature, requested by the user in the same feedback**: an "Asset Rules" button in the Asset Upload page header opens a view-only "Asset Management Settings" modal listing every naming/validation/resolution rule the app enforces (Sponsor Ads, all OVR Trigger types, All Sponsor Logo, RPI, Default Assets) - available to every authenticated role. Backend: `GET /api/asset-rules` (`server/src/assetRules/routes.js`), single-sourced from the same constants the real validation code runs against (`templateConfig.js`, `ALL_SPONSOR_LOGO`, `IMAGE_SPEC`, `RPI_RESOLUTION`, `FORBIDDEN_FILENAME_TERMS`), so this reference view can never drift out of sync with actual behavior. Frontend: `asset-rules.service.ts` + `asset-rules-modal.component.ts/html/scss`. Per the user's explicit request, this is view-only for now - Step 5 is expected to add Edit/Save, restricted to Admin/Super Admin.
  - **Deliberately scoped to the Asset Upload page only, not "all pages" literally**: the user's width complaint said "why not use the full screen for all pages," but Login, Change Password, Create Event, and the Dashboard welcome placeholder are all simple single-column forms/actions (380-640px), not dense data-management screens - widening them to ~1600px would leave a tiny form floating in a large empty page, a real UX regression, not an improvement. The Dashboard is also a known placeholder pending Step 6's real Event List. Left unchanged; flagged here for the user's correction if they specifically want any of these widened too.
  - **Live-verified in a real browser** against the same real sample event `1000 - Star contender Doha`, Table 1 (read-only again, no changes to its content): Sponsor Ads tab shows zero truncated filenames with the icon-only duration button on video rows; OVR Match Triggers tab shows every slot as a compact card with a correctly-differentiated thumbnail (real image crop vs. video glyph vs. empty placeholder) and properly secondary-styled (not solid-orange) action buttons; Default Assets - Main LED renders as two compact cards instead of two sprawling rows; the new Asset Rules modal opens, scrolls, displays every section (Sponsor Ads, the full OVR Trigger table, All Sponsor Logo, RPI, Default Assets) correctly, and closes on Escape or the × button; no JavaScript console errors throughout.
  - **Three items from the same feedback message were already fixed and verified in Step 3** (resolution validation from `event_tables`, the "Save Sequence" button's top-of-section position, and the rejected-upload error message clearing on Save Sequence) - confirmed unaffected by this pass (the touched files are different) and still working per Step 3's own QA doc (TC-26/27/28). If the user is still seeing these live, it's worth a fresh repro since the code hasn't regressed.
  - **Captured for Step 5, not implemented now** (no edit UI exists yet for it to attach to): unchecking an existing table's LED type should prompt a delete-confirmation and fully hide that destination's section across Sponsor Ads, Default Assets, and OVR Triggers - see Step 5's section below.
- **Kickoff decisions confirmed 2026-09-17:**
  1. **`asset_management_templates.json` (Web BRD Section 18.2) — original kickoff research incorrectly concluded this "does not exist anywhere."** It actually exists at `references/0forimplementation/web/asset_management_templates.json` — the original search only covered the Desktop app codebase and the `templates` blob container, not this `references/` subtree. **Caught by the independent architect review**, which cross-checked `templateConfig.js` (originally derived from BRD prose alone) against the real file and found one real divergence — Main LED Home Look's image filename — now corrected (see the review section below). `templateConfig.js` is now reconciled against the real file, not just BRD prose. Still implemented as a repo-local module rather than fetched from blob storage — **also corrected**: BRD Section 18 does explicitly say this file "must be stored in the ... Azure Storage Account," so the original justification ("no equivalent requirement for this file") was factually wrong, not just an approximation. This is now logged as a genuine, acknowledged deviation rather than a mis-justified "compliant" reading — a repo-local static module is still reasonable given the config's low volatility, but moving it to blob storage later is easy if the user wants literal compliance.
  2. **All Sponsor Logo (Section 22) filename rule**: exact match `GPMP.png`, not a "contains" rule — user's explicit choice, consistent with every other OVR Trigger type's exact-match filenames (`TO.mp4`, `winner.mp4`, etc.) rather than RPI's one-off "contains" rule.
  3. **New feature, not originally scoped from the BRD's literal text — "Default Assets" per table/destination.** The BRD's Winning Moment fallback ("if `winner.mp4` is not available, use a copy of `default.mp4`") implies a static, pre-existing `default.mp4`/`default.png` template — but neither exists in the templates container, and **the user pointed out these can't be static/global anyway**, since default assets will genuinely differ by event and by per-table/per-destination resolution. Resolved: build a genuine upload feature — one PNG slot + one MP4 slot per (table, destination), for Inner, Outer, *and* Main LED. Any filename uploaded there is saved as `default.png`/`default.mp4` in that destination's folder, validated against that destination's `event_tables` resolution config (reusing the same mechanism built in Step 3). Created proactively at event/table creation time (same pattern as `keepalive.txt`), per the user's explicit choice, rather than only being created reactively when something is later found missing.
  4. **Placement of the Default Assets UI**: my own interpretation of the user's answer, flagged here for correction if wrong — a Default section added to the existing Sponsor Ads tab for Inner/Outer (alongside the sections built in Step 3), and a separate Default section added to the OVR Triggers tab for Main LED (since Main LED has no presence on the Sponsor Ads tab at all).
- **Deviations from plan (if any):**
  - **All Sponsor Logo (Section 22) is rendered on the OVR Match Triggers tab, not the Sponsor Ads tab** — my own UI placement call, not explicitly specified by the BRD or the user. Reasoning: it isn't part of the sponsor ad sequencing/ordering feature Step 3 built (no `seqno`/duration, no `sponsorsequence.csv` row), even though it's visually a sponsor asset — grouped instead with the other one-off/special trigger types (RPI, Default Assets). Flagged for correction if the user wants it moved to the Sponsor Ads tab instead.
- **User go-ahead:** 2026-09-17 — "Commit Step 4" (no separate testing round requested; user opted to test Steps 4/5/6 together). Committed directly to `main` (`294bb4c`) — no dedicated `step-4-*` branch was cut this session (a process deviation from Steps 1-3's branch-per-step pattern, noted here rather than silently done differently), then pushed to `origin/main`.

## Step 5 — Event Configuration – Add / Change / Delete
- **Status:** Not started
- **Completed on:**
- **What was built:**
- **QA Test Case doc:**
- **Security Checklist:**
- **Deviations from plan (if any):**
- **User-provided requirement, captured 2026-09-17 (during Step 4 feedback, ahead of this step's kickoff)**: when an existing table's LED type checkbox (Inner/Outer/Main) is unchecked during editing, the app must prompt "do you want to delete all files uploaded (if any)" before proceeding, and on confirmation that destination's entire section must be fully hidden from both the Sponsor Ads tab and the OVR Match Triggers tab (including its Default Assets section) — not just left visually empty. Not implemented yet: there is no edit UI for an existing event/table's LED configuration anywhere in the app yet (Step 2 only sets it at creation time) - this requirement has nowhere to attach to until this step builds that edit UI. Flagged here so it's not lost, and should be part of this step's kickoff design rather than bolted on afterward. Also applies symmetrically to Editable Asset Management Settings (see the new read-only settings viewer built during Step 4's UI polish pass) - this step is expected to add Edit/Save for that same view, restricted to Admin/SuperAdmin.

## Step 6 — Dashboard and Event List
- **Status:** Not started
- **Includes:** Event List (Active-status filter), Upload Assets shortcut, Export Event action, Log tab (depends on Step 9 — sequence accordingly).
- **Completed on:**
- **What was built:**
- **QA Test Case doc:**
- **Security Checklist:**
- **Deviations from plan (if any):**

## Step 7 — Export Functionality
- **Status:** Not started
- **Includes:** JSON export, ExportGUID generation, `_GUID.json`.
- **Completed on:**
- **What was built:**
- **QA Test Case doc:**
- **Security Checklist:**
- **Deviations from plan (if any):**

## Step 8 — Sponsor Ad Duration Backend Capability
- **Status:** Not started
- **Includes:** On-demand, synchronous video duration retrieval endpoint.
- **Completed on:**
- **What was built:**
- **QA Test Case doc:**
- **Security Checklist:**
- **Deviations from plan (if any):**

## Step 9 — Change Log Email Notification and Event Log Tab
- **Status:** Not started
- **Includes:** Asynchronous batched Change Log Email (Section 29), Event Log Tab backend (feeds Step 6's Log tab UI). Email provider: **Azure Communication Services Email** (confirmed by user).
- **Completed on:**
- **What was built:**
- **QA Test Case doc:**
- **Security Checklist:**
- **Deviations from plan (if any):**

---

## Kickoff decisions (Step 1, confirmed 2026-09-16)

- Azure subscription `b239d404-c0c0-4098-a287-f9983f71d56b`, resource group `rgsportspresentationsource` (existing, reused), storage account `sasportspresentation` (existing, reused, not overwritten).
- App Service tier: Standard (S1). MySQL tier: Standard/General Purpose (`Standard_D2ds_v4`).
- Email provider for Section 29: **Azure Communication Services Email**.
- Repo: standalone repo at `Applications/web` → `github.com/vatsansg/websportspres`, independent of the desktop app's `ovrledassetmanagement` repo.
- Branch strategy: `step-<N>-<name>` branches, merged to `main` and pushed only after user go-ahead. Commit format: `Step <N>: <description>`.
- MySQL network security: firewall rules (Allow Azure services + App Service outbound IPs), not VNet/private endpoint.
- Dev/test MySQL: one shared server for now, two databases (`assetmgmt` / `assetmgmt_dev`).
- Managed Identity: system-assigned, one per App Service slot (not user-assigned).
- Azure AD app registration: WTT IT to complete (numbered instructions handed over in Step 1); no client secret needed (public-client SPA + PKCE, resource-server token validation on the backend).
- MySQL AAD "Directory Readers" gap: resolve via a tenant directory admin granting the role (not by disabling `aad_auth_validate_oids_in_tenant`).

## Overall status

| Step | Status | Go-ahead date | GitHub commit |
|---|---|---|---|
| 1 | Complete | 2026-09-16 | `1ae97de` (pushed to `origin/main` by user 2026-09-16, after Claude Code's safety layer repeatedly blocked the push from this session) |
| 2 | Complete | 2026-09-17 | `a0d2ee1` (merged and pushed to `origin/main`) |
| 3 | Complete | 2026-09-17 | `29f702d` (merged and pushed to `origin/main`) |
| 4 | Complete | 2026-09-17 | `294bb4c` (committed directly to `main`, pushed to `origin/main`) |
| 5 | Not started | | |
| 6 | Not started | | |
| 7 | Not started | | |
| 8 | Not started | | |
| 9 | Not started | | |
